---
phase: 03-error-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/visas/EditarVisaModal.tsx
  - components/pagos/DetallePagoModal.tsx
autonomous: true
requirements: [UX-01]

must_haves:
  truths:
    - "EditarVisaModal muestra error claro cuando el PATCH falla, sin cerrar el modal"
    - "DetallePagoModal muestra error claro cuando el PATCH falla, sin cerrar el modal"
    - "DetallePagoModal lee datos actualizados desde json.data (no json.pago)"
    - "EditarClienteModal sigue funcionando correctamente con json.error (ya migrado)"
  artifacts:
    - path: "components/visas/EditarVisaModal.tsx"
      provides: "Error check corregido para { data, error } API shape"
      contains: "json.error"
    - path: "components/pagos/DetallePagoModal.tsx"
      provides: "Error check + optimistic update corregido para { data, error } API shape"
      contains: "json.data"
  key_links:
    - from: "components/visas/EditarVisaModal.tsx"
      to: "/api/visas/[id]"
      via: "fetch PATCH"
      pattern: "json\\.error"
    - from: "components/pagos/DetallePagoModal.tsx"
      to: "/api/pagos/[id]"
      via: "fetch PATCH"
      pattern: "json\\.data"
---

<objective>
Fix the broken error check pattern in EditarVisaModal and DetallePagoModal so that edit operations never fail silently.

Purpose: Both modals use the old `!json.success` check which is always truthy with the new `{ data, error }` API shape, causing every save (even successful ones) to show an error message.
Output: Two fixed modal components that correctly detect errors via `json.error` and read updated data from `json.data`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-error-feedback/03-CONTEXT.md

<interfaces>
<!-- API response shape (standardized in Phase 2) -->
All PATCH endpoints return:
- Success: { data: <entity>, error: null } with status 200
- Validation error: { data: null, error: "message" } with status 400
- Server error: { data: null, error: "message" } with status 500

<!-- Correct error check pattern (from EditarClienteModal, line 195) -->
```typescript
const json = await res.json() as { data?: unknown; error?: string }

if (!res.ok || json.error) {
  setServerError(json.error ?? 'Error al actualizar el cliente')
  return
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix EditarVisaModal error check pattern</name>
  <files>components/visas/EditarVisaModal.tsx</files>
  <read_first>
    - components/visas/EditarVisaModal.tsx (current broken state)
    - components/clientes/EditarClienteModal.tsx (reference: correct pattern at line 193-198)
  </read_first>
  <action>
    In `components/visas/EditarVisaModal.tsx`, make these exact changes per D-05, D-06, D-09:

    1. **Line 139** — Change the type annotation from:
       `let json: { success?: boolean; error?: string }`
       to:
       `let json: { data?: unknown; error?: string }`

    2. **Line 141** — Update the `as` cast to match:
       `json = await res.json() as { data?: unknown; error?: string }`

    3. **Line 148** — Change the error check from:
       `if (!res.ok || !json.success)`
       to:
       `if (!res.ok || json.error)`

    Per D-09, the success path stays unchanged: `router.refresh()` then `setTimeout(() => setOpen(false), 1200)`. No need to read `json.data` here since the component already uses `router.refresh()` to sync state.

    Per D-07, the error path already sets `setServerError()` and `return` (does not call `setOpen(false)` or `setSaved(true)`), so the modal stays open with error displayed. No change needed there.
  </action>
  <verify>
    <automated>cd "C:/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - EditarVisaModal.tsx line ~139 contains `{ data?: unknown; error?: string }` (not `success`)
    - EditarVisaModal.tsx line ~148 contains `json.error` (not `!json.success`)
    - EditarVisaModal.tsx does NOT contain the string `json.success` anywhere
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>EditarVisaModal correctly checks json.error for failures and no longer references json.success</done>
</task>

<task type="auto">
  <name>Task 2: Fix DetallePagoModal error check and data access pattern</name>
  <files>components/pagos/DetallePagoModal.tsx</files>
  <read_first>
    - components/pagos/DetallePagoModal.tsx (current broken state)
    - components/clientes/EditarClienteModal.tsx (reference: correct pattern at line 193-198)
  </read_first>
  <action>
    In `components/pagos/DetallePagoModal.tsx`, make these exact changes per D-05, D-06, D-08, D-10:

    1. **Line 126** — Change the type annotation from:
       `const json = await res.json() as { success?: boolean; error?: string; pago?: Record<string, unknown> }`
       to:
       `const json = await res.json() as { data?: Record<string, unknown> | null; error?: string }`

    2. **Line 128** — Change the error check from:
       `if (!res.ok || !json.success)`
       to:
       `if (!res.ok || json.error)`

    3. **Lines 138-139** — Change the optimistic update data access from:
       `fecha_pago: (json.pago?.fecha_pago as string | null) ?? pago.fecha_pago,`
       `fecha_vencimiento_deuda: (json.pago?.fecha_vencimiento_deuda as string | null) ?? null,`
       to (per D-10):
       `fecha_pago: (json.data?.fecha_pago as string | null) ?? pago.fecha_pago,`
       `fecha_vencimiento_deuda: (json.data?.fecha_vencimiento_deuda as string | null) ?? null,`

    Per D-07, the error path already sets `setError()` and `return` (does not call `onUpdated()` or `setSuccess(true)`), so the modal stays open with error displayed. No change needed there.
  </action>
  <verify>
    <automated>cd "C:/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DetallePagoModal.tsx line ~126 contains `{ data?: Record<string, unknown> | null; error?: string }` (not `success` or `pago`)
    - DetallePagoModal.tsx line ~128 contains `json.error` (not `!json.success`)
    - DetallePagoModal.tsx contains `json.data?.fecha_pago` (not `json.pago?.fecha_pago`)
    - DetallePagoModal.tsx contains `json.data?.fecha_vencimiento_deuda` (not `json.pago?.fecha_vencimiento_deuda`)
    - DetallePagoModal.tsx does NOT contain the string `json.success` anywhere
    - DetallePagoModal.tsx does NOT contain the string `json.pago` anywhere (except the unrelated `pago` prop references)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>DetallePagoModal correctly checks json.error for failures and reads updated data from json.data instead of json.pago</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `EditarVisaModal.tsx` contains `json.error` and does NOT contain `json.success`
3. `DetallePagoModal.tsx` contains `json.error` and `json.data` — does NOT contain `json.success` or `json.pago?.fecha`
4. `EditarClienteModal.tsx` remains unchanged (already correct from Phase 2)
</verification>

<success_criteria>
- All three edit modals (clientes, visas, pagos) use the `{ data, error }` response pattern
- No edit modal references `json.success` or `json.pago`
- Build passes cleanly
- UX-01 is satisfied: no edit operation fails silently
</success_criteria>

<output>
After completion, create `.planning/phases/03-error-feedback/03-01-SUMMARY.md`
</output>
