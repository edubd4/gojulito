---
phase: 13-formularios-dropdowns-fecha-turno
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/clientes/ClientesTable.tsx
  - components/clientes/EditarClienteModal.tsx
  - components/clientes/NuevoClienteModal.tsx
  - components/configuracion/EditarUsuarioModal.tsx
  - components/pagos/NuevoPagoModal.tsx
  - components/pagos/PagosTable.tsx
  - components/seminarios/AgregarAsistenteModal.tsx
  - components/seminarios/EditarAsistenteModal.tsx
  - components/seminarios/EditarSeminarioModal.tsx
  - components/seminarios/LogisticaSection.tsx
  - components/seminarios/NuevoSeminarioModal.tsx
  - components/tramites/steps/StepDatosPersonales.tsx
  - components/tramites/steps/StepPasaporte.tsx
  - components/tramites/TramitesTable.tsx
  - components/visas/EditarVisaModal.tsx
  - components/visas/IniciarVisaModal.tsx
  - components/visas/NuevoTramiteModal.tsx
  - components/tramites/NuevoTramiteWizard.tsx
  - components/tramites/steps/StepVisaDS160.tsx
autonomous: false
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "Todos los select del sistema muestran fondo oscuro en Chrome/Edge (sin fondo blanco del OS)"
    - "En el wizard paso 4, al seleccionar TURNO_ASIGNADO aparece un campo de fecha"
    - "Al seleccionar EN_PROCESO el campo de fecha no aparece (oculto correctamente)"
    - "La fecha ingresada en el campo turno se envia a la API al crear el tramite"
  artifacts:
    - path: "components/tramites/NuevoTramiteWizard.tsx"
      provides: "WizardData con fecha_turno, INITIAL_DATA actualizado, body.fecha_turno enviado cuando TURNO_ASIGNADO"
    - path: "components/tramites/steps/StepVisaDS160.tsx"
      provides: "Campo input type=date condicional con colorScheme dark, visible solo cuando estado_visa_inicial === TURNO_ASIGNADO"
  key_links:
    - from: "StepVisaDS160.tsx"
      to: "NuevoTramiteWizard.tsx WizardData.fecha_turno"
      via: "onChange({ fecha_turno: e.target.value })"
    - from: "NuevoTramiteWizard.tsx handleStep4Submit"
      to: "/api/visas"
      via: "body.fecha_turno incluido cuando estado_visa_inicial === TURNO_ASIGNADO"
---

<objective>
Corregir dos bugs de formularios en GoJulito:

1. FIX-01: Todos los `<select>` nativos muestran fondo blanco en Chrome/Edge porque el OS aplica el tema claro. Solucion: agregar `style={{ colorScheme: 'dark' }}` a cada select que lo requiere (~46 selects en 17 archivos).

2. FIX-02: El wizard NuevoTramiteWizard paso 4 no tiene campo para ingresar la fecha del turno cuando el usuario selecciona TURNO_ASIGNADO. Solucion: mostrar condicionalmente un input date y enviarlo a la API.

Purpose: Evitar que el sistema operativo sobreescriba el fondo de los dropdowns, y permitir registrar la fecha de turno desde el flujo de creacion.
Output: 17 archivos actualizados (FIX-01) + 2 archivos del wizard (FIX-02). Sin cambios en la API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-formularios-dropdowns-fecha-turno/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-01 — Agregar colorScheme dark a todos los selects faltantes</name>

  <files>
    components/clientes/ClientesTable.tsx
    components/clientes/EditarClienteModal.tsx
    components/clientes/NuevoClienteModal.tsx
    components/configuracion/EditarUsuarioModal.tsx
    components/pagos/NuevoPagoModal.tsx
    components/pagos/PagosTable.tsx
    components/seminarios/AgregarAsistenteModal.tsx
    components/seminarios/EditarAsistenteModal.tsx
    components/seminarios/EditarSeminarioModal.tsx
    components/seminarios/LogisticaSection.tsx
    components/seminarios/NuevoSeminarioModal.tsx
    components/tramites/steps/StepDatosPersonales.tsx
    components/tramites/steps/StepPasaporte.tsx
    components/tramites/TramitesTable.tsx
    components/visas/EditarVisaModal.tsx
    components/visas/IniciarVisaModal.tsx
    components/visas/NuevoTramiteModal.tsx
  </files>

  <action>
Leer cada archivo y agregar `style={{ colorScheme: 'dark' }}` a los `&lt;select&gt;` que no lo tienen aun.

REGLA: Si el select ya tiene `style={{...}}`, hacer merge de la propiedad `colorScheme: 'dark'` dentro del objeto existente. Si no tiene `style`, agregar `style={{ colorScheme: 'dark' }}` como nuevo prop.

NO tocar los selects en estos archivos (ya tienen colorScheme):
- components/clientes/RegistrarPagoModal.tsx
- components/grupos/AccionLoteGrupoModal.tsx
- components/pagos/CambiarEstadoPagoDialog.tsx
- components/pagos/DetallePagoModal.tsx
- components/pagos/FechaVencimientoDialog.tsx

Referencia exacta por archivo (lineas aproximadas segun snapshot del 2026-04-06 — verificar al leer cada archivo antes de editar):

components/clientes/ClientesTable.tsx — 9 selects sin colorScheme (lineas ~437, 447, 461, 472, 634, 646, 759, 781, 803). Patron de edicion: agregar style={{ colorScheme: 'dark' }} como prop adicional al elemento select existente, sin modificar className ni logica.

components/clientes/EditarClienteModal.tsx — 3 selects faltantes (lineas ~344, 385, 413, 431). NO tocar linea ~376 (ya tiene colorScheme).

components/clientes/NuevoClienteModal.tsx — 2 selects faltantes (lineas ~268, 311, 334). NO tocar linea ~300 (ya tiene colorScheme).

components/configuracion/EditarUsuarioModal.tsx — 1 select (linea ~104).

components/pagos/NuevoPagoModal.tsx — 4 selects (lineas ~198, 299, 318, 355).

components/pagos/PagosTable.tsx — 2 selects (lineas ~277, 287).

components/seminarios/AgregarAsistenteModal.tsx — 4 selects (lineas ~212, 274, 290, 333).

components/seminarios/EditarAsistenteModal.tsx — 4 selects (lineas ~169, 195, 224, 238).

components/seminarios/EditarSeminarioModal.tsx — 1 select (linea ~168).

components/seminarios/LogisticaSection.tsx — 2 selects (lineas ~313, 349).

components/seminarios/NuevoSeminarioModal.tsx — 1 select (linea ~154).

components/tramites/steps/StepDatosPersonales.tsx — 1 select (linea ~43).

components/tramites/steps/StepPasaporte.tsx — 1 select (linea ~42).

components/tramites/TramitesTable.tsx — 2 selects (lineas ~281, 292).

components/visas/EditarVisaModal.tsx — 1 select (linea ~199).

components/visas/IniciarVisaModal.tsx — 1 select (linea ~200).

components/visas/NuevoTramiteModal.tsx — 3 selects (lineas ~350, 384, 411).

Workflow recomendado: leer el archivo, localizar todos los elementos `&lt;select`, verificar cuales carecen de colorScheme, agregar el prop, guardar. No modificar nada mas (no tocar classNames, logica, ni otros atributos).
  </action>

  <verify>
    <automated>cd C:/Users/edu_i/EduWorkspace/Clientes-Code/Gojulito && npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
- El build de Next.js completa sin errores TypeScript ni de compilacion.
- Ningun `&lt;select&gt;` en los 17 archivos listados carece de `style={{ colorScheme: 'dark' }}`.
- Los archivos que ya tenian colorScheme no fueron modificados.
  </done>
</task>

<task type="auto">
  <name>Task 2: FIX-02 — Campo fecha_turno condicional en wizard paso 4</name>

  <files>
    components/tramites/NuevoTramiteWizard.tsx
    components/tramites/steps/StepVisaDS160.tsx
  </files>

  <action>
Realizar tres cambios en dos archivos para agregar el campo fecha_turno al wizard.

Estado actual de NuevoTramiteWizard.tsx (leido el 2026-04-06):
- Linea 15: interfaz WizardData
- Linea 40: ultimo campo de Step 4 es `estado_visa_inicial: 'EN_PROCESO' | 'TURNO_ASIGNADO'`
- Linea 43: objeto INITIAL_DATA
- Linea 64: `estado_visa_inicial: 'EN_PROCESO',` (ultimo campo de INITIAL_DATA)
- Linea 166: funcion handleStep4Submit
- Linea 188-191: bloque de asignacion condicional del body (ds160, email_portal, orden_atencion, notas)
- Linea 193: `const res = await fetch('/api/visas', ...)`

Estado actual de StepVisaDS160.tsx (leido el 2026-04-06):
- Linea 130-160: bloque md:col-span-2 con label "Estado Inicial del Tramite" y radio buttons
- Linea 160: cierre del div del bloque de estado (ultimo `</div>` del selector)
- Linea 163: apertura del info card `<div className="md:col-span-2 bg-gj-surface-mid...`

--- CAMBIO A: NuevoTramiteWizard.tsx ---

Cambio 1 — Agregar campo a la interfaz WizardData despues de la linea con estado_visa_inicial (~linea 40):
  Insertar: `fecha_turno: string`

Cambio 2 — Agregar valor inicial en INITIAL_DATA despues de estado_visa_inicial: 'EN_PROCESO', (~linea 64):
  Insertar: `fecha_turno: '',`

Cambio 3 — En handleStep4Submit, despues de `if (notas) body.notas = notas` (~linea 191) y ANTES de `const res = await fetch('/api/visas', ...` (~linea 193), insertar:
  `if (data.estado_visa_inicial === 'TURNO_ASIGNADO' && data.fecha_turno.trim()) {`
  `  body.fecha_turno = data.fecha_turno.trim()`
  `}`

--- CAMBIO B: StepVisaDS160.tsx ---

Insertar el siguiente bloque JSX ENTRE el cierre del div del status selector (linea ~160) y la apertura del info card (linea ~163):

  {data.estado_visa_inicial === 'TURNO_ASIGNADO' && (
    &lt;div className="md:col-span-2"&gt;
      &lt;Field label="Fecha del Turno" optional&gt;
        &lt;Input
          type="date"
          value={data.fecha_turno}
          onChange={(e) => onChange({ fecha_turno: e.target.value })}
          style={{ colorScheme: 'dark' }}
        /&gt;
      &lt;/Field&gt;
    &lt;/div&gt;
  )}

NOTAS:
- El componente Input local (linea 29-37 de StepVisaDS160.tsx) usa spread `{...props}` al renderizar el `&lt;input&gt;` nativo, por lo que `style={{ colorScheme: 'dark' }}` se aplica correctamente al picker nativo del date.
- El campo no es required — el turno puede crearse sin fecha.
- No agregar validacion de fecha futura ni animaciones de transicion — out of scope.
- La API en app/api/visas/route.ts ya acepta fecha_turno (linea 119) — NO modificar ese archivo.
  </action>

  <verify>
    <automated>cd C:/Users/edu_i/EduWorkspace/Clientes-Code/Gojulito && npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
- El build completa sin errores TypeScript.
- La interfaz WizardData incluye `fecha_turno: string`.
- INITIAL_DATA incluye `fecha_turno: ''`.
- handleStep4Submit envia `body.fecha_turno` solo cuando `estado_visa_inicial === 'TURNO_ASIGNADO'` y la fecha no esta vacia.
- StepVisaDS160 muestra el campo date cuando `estado_visa_inicial === 'TURNO_ASIGNADO'` y lo oculta cuando es `EN_PROCESO`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacion visual — dropdowns oscuros y campo fecha turno</name>

  <files>ninguno</files>

  <action>
Iniciar servidor de desarrollo con `npm run dev` y verificar visualmente los dos fixes segun los pasos de how-to-verify.
  </action>

  <what-built>
  - FIX-01: Agregado style colorScheme dark a todos los selects faltantes en 17 archivos (~46 selects).
  - FIX-02: Campo fecha_turno condicional en wizard paso 4, con envio a la API cuando TURNO_ASIGNADO.
  </what-built>

  <how-to-verify>
  Iniciar el servidor: npm run dev

  VERIFICAR FIX-01 — Dropdowns oscuros:

  1. Abrir http://localhost:3000/clientes
     - Hacer clic en los filtros tipo select (estado, canal, etc.)
     - ESPERADO: fondo negro/oscuro en el dropdown nativo. FALLO: fondo blanco.

  2. Abrir cualquier modal de edicion de cliente (boton editar en la tabla)
     - Abrir los selects dentro del modal
     - ESPERADO: fondo oscuro consistente con el diseno.

  3. Abrir http://localhost:3000/seminarios
     - Verificar selects en los modales de asistentes (Agregar Asistente, Editar Asistente)
     - ESPERADO: fondo oscuro.

  4. Abrir http://localhost:3000/tramites
     - Verificar selects en los filtros de la tabla y en los modales de visa
     - ESPERADO: fondo oscuro.

  VERIFICAR FIX-02 — Campo fecha turno en wizard:

  5. Abrir http://localhost:3000/tramites/nuevo
     - Completar pasos 1, 2, 3 con datos de prueba (puede ser datos ficticios)
     - En paso 4 (Visa DS-160), verificar que "En Proceso" esta seleccionado por defecto
     - ESPERADO: NO aparece campo de fecha.

  6. Hacer clic en "Turno Asignado" en el paso 4
     - ESPERADO: aparece un campo "Fecha del Turno" con input type=date, fondo oscuro.
     - Volver a seleccionar "En Proceso"
     - ESPERADO: el campo de fecha desaparece.

  7. Seleccionar "Turno Asignado", ingresar una fecha, hacer clic en "Crear Tramite"
     - ESPERADO: tramite creado exitosamente.
     - Navegar al detalle del tramite recien creado en /tramites
     - ESPERADO: la fecha del turno esta registrada en el sistema.

  8. Crear un tramite con "En Proceso" (sin fecha)
     - ESPERADO: tramite creado sin errores, sin campos de fecha.
  </how-to-verify>

  <verify>
    <automated>cd C:/Users/edu_i/EduWorkspace/Clientes-Code/Gojulito && npm run build 2>&1 | tail -5</automated>
  </verify>

  <done>
Todos los selects del sistema muestran fondo oscuro. El campo fecha turno aparece y desaparece correctamente segun el estado seleccionado. La fecha se guarda al crear el tramite.
  </done>

  <resume-signal>
  Escribir "aprobado" si todo funciona correctamente, o describir los problemas encontrados para que el executor los corrija.
  </resume-signal>
</task>

</tasks>

<verification>
- npm run build pasa sin errores TypeScript ni de compilacion.
- Todos los select en los 17 archivos listados tienen style={{ colorScheme: 'dark' }}.
- WizardData.fecha_turno existe como string.
- INITIAL_DATA.fecha_turno es ''.
- El campo date aparece en StepVisaDS160 cuando estado_visa_inicial === 'TURNO_ASIGNADO'.
- El campo date desaparece cuando estado_visa_inicial === 'EN_PROCESO'.
- handleStep4Submit incluye fecha_turno en el body solo cuando TURNO_ASIGNADO y fecha no vacia.
- app/api/visas/route.ts no fue modificado.
</verification>

<success_criteria>
- Build sin errores.
- Todos los dropdowns del sistema muestran fondo oscuro en Chrome y Edge.
- En el wizard paso 4, seleccionar TURNO_ASIGNADO muestra el campo de fecha; seleccionar EN_PROCESO lo oculta.
- La fecha del turno se guarda en la base de datos al crear el tramite.
- No se modificaron los selectores que ya tenian colorScheme (RegistrarPagoModal, AccionLoteGrupoModal, CambiarEstadoPagoDialog, DetallePagoModal, FechaVencimientoDialog).
- No se hicieron cambios en app/api/visas/route.ts.
</success_criteria>

<output>
Despues de completar todos los tasks, crear .planning/phases/13-formularios-dropdowns-fecha-turno/13-01-SUMMARY.md con:
- Archivos modificados
- Cambios realizados por task
- Resultado del build
- Cualquier desviacion del plan
</output>
