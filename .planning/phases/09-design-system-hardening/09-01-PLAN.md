---
phase: 09-design-system-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.ts
autonomous: true
requirements:
  - DS-03
must_haves:
  truths:
    - "The color #a78bfa is available as gj-seminario in Tailwind classes (text-gj-seminario, bg-gj-seminario, border-gj-seminario)"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "gj-seminario token"
      contains: "seminario"
  key_links:
    - from: "tailwind.config.ts"
      to: "components/calendario/CalendarioView.tsx"
      via: "Tailwind class compilation"
      pattern: "gj-seminario"
---

<objective>
Register the `gj-seminario` color token (`#a78bfa`) in `tailwind.config.ts` so that all subsequent migration plans can use `text-gj-seminario`, `bg-gj-seminario`, and `bg-gj-seminario/[18%]` classes.

Purpose: This is a prerequisite for plans 02-05 which will replace inline hex values with Tailwind classes. The seminario purple color was introduced in Phase 07 but never added to the config.
Output: Updated `tailwind.config.ts` with the new token.
</objective>

<execution_context>
@C:\Users\edu_i\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edu_i\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-hardening/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gj-seminario token to tailwind.config.ts</name>
  <files>tailwind.config.ts</files>
  <read_first>
    - tailwind.config.ts
  </read_first>
  <action>
In `tailwind.config.ts`, inside the `gj` object under `theme.extend.colors`, add the seminario token after the `secondary` entry:

```typescript
gj: {
  bg: "#0b1628",
  card: "#111f38",
  input: "#172645",
  amber: "#e8a020",
  green: "#22c97a",
  red: "#e85a5a",
  blue: "#4a9eff",
  text: "#e8e6e0",
  secondary: "#9ba8bb",
  seminario: "#a78bfa",  // purple — chips de seminario en calendario
},
```

Only add the `seminario: "#a78bfa"` line with the comment. Do not modify any other tokens or config.

After modifying, run `npm run build` to verify the project compiles cleanly with the new token.
  </action>
  <verify>
    <automated>cd C:/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && grep "seminario" tailwind.config.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "seminario.*#a78bfa" tailwind.config.ts` outputs exactly 1 line
    - `npm run build` exits with code 0 (no TypeScript or build errors)
    - All existing gj tokens remain unchanged (bg, card, input, amber, green, red, blue, text, secondary)
  </acceptance_criteria>
  <done>tailwind.config.ts contains gj-seminario token, build compiles cleanly</done>
</task>

</tasks>

<verification>
- `grep "seminario.*#a78bfa" tailwind.config.ts` returns 1 match
- `npm run build` passes
</verification>

<success_criteria>
The gj-seminario token is registered in tailwind.config.ts and the build succeeds. All subsequent plans can use text-gj-seminario, bg-gj-seminario, bg-gj-seminario/[18%] classes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-hardening/09-01-SUMMARY.md`
</output>
