---
phase: 09-design-system-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - components/seminarios/NuevoSeminarioModal.tsx
  - components/seminarios/EditarSeminarioModal.tsx
  - components/seminarios/AgregarAsistenteModal.tsx
  - components/seminarios/EditarAsistenteModal.tsx
  - components/seminarios/AsistentesTable.tsx
  - components/seminarios/InactivarSeminarioButton.tsx
  - components/seminarios/SeminarioCard.tsx
  - components/tramites/NuevoTramiteModal.tsx
  - components/tramites/TramitesTable.tsx
  - components/visas/IniciarVisaModal.tsx
  - components/visas/EditarVisaModal.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-04
must_haves:
  truths:
    - "No hex color values exist in style={} props in any seminarios, tramites, or visas component"
    - "All inputs and textareas in seminarios/tramites/visas modals have focus:ring-2 focus:ring-gj-amber"
    - "BADGE maps use Tailwind class strings (classes property), not hex color/bg strings"
    - "SeminarioCard hover state uses Tailwind hover: variants, not JS state"
    - "Dynamic error borders in IniciarVisaModal and EditarVisaModal use conditional className"
    - "Radio buttons with accentColor use accent-gj-amber Tailwind class"
  artifacts:
    - path: "components/seminarios/SeminarioCard.tsx"
      provides: "Migrated card with Tailwind hover state (no JS hover state)"
    - path: "components/visas/IniciarVisaModal.tsx"
      provides: "Migrated visa modal with conditional error borders"
    - path: "components/visas/EditarVisaModal.tsx"
      provides: "Migrated edit visa modal with conditional error borders"
    - path: "components/tramites/TramitesTable.tsx"
      provides: "Migrated tramites table with BADGE map as class strings"
  key_links:
    - from: "components/seminarios/SeminarioCard.tsx"
      to: "tailwind.config.ts"
      via: "hover:border-white/[14%] references white opacity utility"
      pattern: "hover:border-white"
    - from: "components/visas/IniciarVisaModal.tsx"
      to: "tailwind.config.ts"
      via: "border-gj-red references gj-red token"
      pattern: "border-gj-red"
---

<objective>
Migrate all 11 files in `components/seminarios/`, `components/tramites/`, and `components/visas/` from inline `style={}` with hex colors to Tailwind `gj-*` classes.

Purpose: These three component groups share the same inputStyle/labelStyle patterns and BADGE maps. Grouping them in one plan avoids context switching on identical mechanical patterns.
Output: 11 migrated component files with zero hex in style props (except colorScheme: dark on date inputs and boxShadow on modals/dropdowns). SeminarioCard no longer uses JS hover state. Dynamic error borders use conditional className.
</objective>

<execution_context>
@C:\Users\edu_i\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edu_i\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-hardening/08-CONTEXT.md
@.planning/phases/08-design-system-hardening/08-RESEARCH.md

<interfaces>
<!-- Hex-to-Tailwind mapping (locked in CONTEXT.md — per D-02): -->
| Hex / rgba | Tailwind class |
|---|---|
| `#0b1628` | `bg-gj-bg` |
| `#111f38` | `bg-gj-card` |
| `#172645` | `bg-gj-input` |
| `#e8a020` | `text-gj-amber` / `bg-gj-amber` / `border-gj-amber` / `accent-gj-amber` |
| `#22c97a` | `text-gj-green` / `bg-gj-green` |
| `#e85a5a` | `text-gj-red` / `bg-gj-red` / `border-gj-red` |
| `#4a9eff` | `text-gj-blue` / `bg-gj-blue` |
| `#a78bfa` | `text-gj-seminario` / `bg-gj-seminario` (registered in plan 09-01) |
| `#e8e6e0` | `text-gj-text` |
| `#9ba8bb` | `text-gj-secondary` |
| `rgba(255,255,255,0.1)` | `border-white/10` |
| `rgba(255,255,255,0.06)` | `border-white/[6%]` |
| `rgba(255,255,255,0.07)` | `border-white/[7%]` |
| `rgba(255,255,255,0.14)` | `border-white/[14%]` |
| `rgba(0,0,0,0.55)` | `bg-black/55` |
| `rgba(232,160,32,0.15)` | `bg-gj-amber/15` |
| `rgba(34,201,122,0.15)` | `bg-gj-green/15` |
| `rgba(232,90,90,0.08)` | `bg-gj-red/[8%]` |
| `rgba(232,90,90,0.15)` | `bg-gj-red/15` |
| `rgba(74,158,255,0.15)` | `bg-gj-blue/15` |
| `rgba(155,168,187,0.15)` | `bg-gj-secondary/15` |
| `rgba(167,139,250,0.18)` | `bg-gj-seminario/[18%]` |

<!-- Standard replacement classes (per D-02): -->
inputStyle object -> className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
labelStyle object -> className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
Date input: keep style={{ colorScheme: 'dark' }} as ONLY remaining style prop
Select: add cursor-pointer to className
Radio/checkbox with accentColor: '#e8a020' -> add accent-gj-amber to className, remove from style

<!-- SeminarioCard hover state migration (per research pattern): -->
Before (JS state):
  const [hovered, setHovered] = useState(false)
  style={{ border: `1px solid ${hovered ? 'rgba(255,255,255,0.14)' : 'rgba(255,255,255,0.06)'}` }}
After (Tailwind hover:):
  className="border border-white/[6%] hover:border-white/[14%] transition-colors cursor-pointer bg-gj-card rounded-xl p-5 sm:p-6"
  Remove the useState for hover entirely.
  Remove onMouseEnter and onMouseLeave handlers from the card element.

<!-- Dynamic error border in IniciarVisaModal and EditarVisaModal: -->
Before: style={{ ...inputStyle, borderColor: errors.campo ? '#e85a5a' : 'rgba(255,255,255,0.1)' }}
After:  className={`w-full bg-gj-input text-gj-text border rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none ${errors.campo ? 'border-gj-red' : 'border-white/10'}`}

<!-- BADGE maps for AsistentesTable and TramitesTable: -->
Convirtio badge (AsistentesTable):
  SI:            { classes: 'text-gj-green bg-gj-green/15',         label: 'Sí' }
  NO:            { classes: 'text-gj-red bg-gj-red/15',             label: 'No' }
  EN_SEGUIMIENTO:{ classes: 'text-gj-amber bg-gj-amber/15',         label: 'En Seguimiento' }

EstadoPago badge (AsistentesTable):
  PAGADO:    { classes: 'text-gj-green bg-gj-green/15',   label: 'Pagado' }
  DEUDA:     { classes: 'text-gj-red bg-gj-red/15',       label: 'Deuda' }
  PENDIENTE: { classes: 'text-gj-amber bg-gj-amber/15',   label: 'Pendiente' }

SeminarioCard modalidad badge:
  PRESENCIAL: { classes: 'text-gj-green bg-gj-green/15',   label: 'Presencial' }
  VIRTUAL:    { classes: 'text-gj-blue bg-gj-blue/15',     label: 'Virtual' }

TramitesTable estado badge — use same EstadoPago pattern above.

Badge render pattern:
<span className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${badge.classes}`}>{badge.label}</span>

<!-- Properties that MUST stay as style={}: -->
1. colorScheme: 'dark' — date inputs only
2. boxShadow with complex values — modals and dropdowns (InactivarSeminarioButton confirmation modal also has boxShadow)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 seminarios modals + InactivarSeminarioButton + SeminarioCard</name>
  <files>
    components/seminarios/NuevoSeminarioModal.tsx
    components/seminarios/EditarSeminarioModal.tsx
    components/seminarios/AgregarAsistenteModal.tsx
    components/seminarios/EditarAsistenteModal.tsx
    components/seminarios/InactivarSeminarioButton.tsx
    components/seminarios/SeminarioCard.tsx
  </files>
  <read_first>
    - components/seminarios/NuevoSeminarioModal.tsx
    - components/seminarios/EditarSeminarioModal.tsx
    - components/seminarios/AgregarAsistenteModal.tsx
    - components/seminarios/EditarAsistenteModal.tsx
    - components/seminarios/InactivarSeminarioButton.tsx
    - components/seminarios/SeminarioCard.tsx
  </read_first>
  <action>
**For NuevoSeminarioModal, EditarSeminarioModal, AgregarAsistenteModal, EditarAsistenteModal:**

1. Delete `inputStyle` (and `labelStyle` if present) objects.
2. Replace every `style={inputStyle}` with:
   ```
   className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
   ```
   Date inputs: keep `style={{ colorScheme: 'dark' }}` as the only style prop.
   Select elements: add `cursor-pointer` to the className.
3. Replace `style={labelStyle}` with:
   ```
   className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
   ```
4. Migrate all remaining `style={}` color props (hex and rgba) to Tailwind classes using the mapping in the interfaces block.
5. Migrate non-color style props (fontSize, fontWeight, padding, gap, borderRadius, display, position, etc.) to Tailwind utilities using the same scale as plan 09-02.
6. Modal backdrop/panel: `className="fixed inset-0 z-[60] bg-black/55"` / panel with `style={{ boxShadow: ... }}`.
7. Remove empty `style={}` attributes.

---

**For InactivarSeminarioButton:**
This component renders an inline confirmation UI (not a full modal backdrop). Apply the same color migration:
- Replace hex colors in any `style={}` with Tailwind classes
- Keep `boxShadow` in `style={}` if the confirmation container uses one
- Migrate non-color style props

---

**For SeminarioCard — hover state refactor (per D-02 and research):**

1. Find the `useState(false)` for hover state (something like `const [hovered, setHovered] = useState(false)`).
2. Remove this state variable.
3. Remove `onMouseEnter` and `onMouseLeave` handlers that set hover state.
4. Replace the `style={{ border: ... }}` that conditionally applies border based on hovered state with:
   ```
   className="border border-white/[6%] hover:border-white/[14%] transition-colors cursor-pointer bg-gj-card rounded-xl p-5 sm:p-6"
   ```
   (Adjust other layout classes as found in the actual file — preserve the card's structure.)
5. Migrate BADGE_MODALIDAD map from hex to class strings:
   ```typescript
   const BADGE_MODALIDAD = {
     PRESENCIAL: { classes: 'text-gj-green bg-gj-green/15',   label: 'Presencial' },
     VIRTUAL:    { classes: 'text-gj-blue bg-gj-blue/15',     label: 'Virtual' },
   }
   ```
   Update badge render to: `<span className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${BADGE_MODALIDAD[modalidad].classes}`}>`
6. Migrate all remaining hex/rgba color style props to Tailwind classes.
7. Migrate non-color style props to Tailwind utilities.
8. Remove empty `style={}` attributes.

Run `npm run build` after all 6 files.
  </action>
  <verify>
    <automated>cd /c/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -n "style={{" components/seminarios/NuevoSeminarioModal.tsx components/seminarios/EditarSeminarioModal.tsx components/seminarios/AgregarAsistenteModal.tsx components/seminarios/EditarAsistenteModal.tsx components/seminarios/InactivarSeminarioButton.tsx components/seminarios/SeminarioCard.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "inputStyle" components/seminarios/NuevoSeminarioModal.tsx` outputs 0
    - `grep -c "inputStyle" components/seminarios/EditarSeminarioModal.tsx` outputs 0
    - `grep -c "inputStyle" components/seminarios/AgregarAsistenteModal.tsx` outputs 0
    - `grep -c "onMouseEnter" components/seminarios/SeminarioCard.tsx` outputs 0 (hover JS state removed)
    - `grep -c "hover:border-white" components/seminarios/SeminarioCard.tsx` outputs at least 1 (Tailwind hover class added)
    - `grep "BADGE_MODALIDAD" components/seminarios/SeminarioCard.tsx | grep -c "classes"` outputs at least 1
    - `grep -c "focus:ring-gj-amber" components/seminarios/NuevoSeminarioModal.tsx` outputs at least 1
    - All remaining `style={{` in these 6 files contain ONLY colorScheme or boxShadow
  </acceptance_criteria>
  <done>6 seminarios components migrated: modals use Tailwind classes, SeminarioCard uses hover: variant, BADGE_MODALIDAD uses class strings, all inputs have focus rings</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AsistentesTable, NuevoTramiteModal, TramitesTable, IniciarVisaModal, EditarVisaModal</name>
  <files>
    components/seminarios/AsistentesTable.tsx
    components/tramites/NuevoTramiteModal.tsx
    components/tramites/TramitesTable.tsx
    components/visas/IniciarVisaModal.tsx
    components/visas/EditarVisaModal.tsx
  </files>
  <read_first>
    - components/seminarios/AsistentesTable.tsx
    - components/tramites/NuevoTramiteModal.tsx
    - components/tramites/TramitesTable.tsx
    - components/visas/IniciarVisaModal.tsx
    - components/visas/EditarVisaModal.tsx
  </read_first>
  <action>
**AsistentesTable.tsx:**
1. Migrate inline BADGE map (convirtio + estado_pago) from hex to class strings using mapping in interfaces block.
2. Update badge render sites to `className={...badge.classes...}`.
3. Migrate all `style={}` color props (hex and rgba) to Tailwind classes.
4. Keep `boxShadow` in `style={}` for any dropdown panels.
5. Migrate non-color style props to Tailwind utilities.
6. Remove empty `style={}`.

---

**NuevoTramiteModal.tsx:**
1. Delete `inputStyle` (and `labelStyle` if present).
2. Replace with standard className pattern.
3. Date inputs: keep `style={{ colorScheme: 'dark' }}`.
4. Migrate all color and non-color style props.
5. Modal backdrop/panel pattern applies.

---

**TramitesTable.tsx:**
1. Migrate BADGE_ESTADO inline map from hex to class strings.
2. Update badge render sites.
3. The table has multiple date filter inputs — apply inputStyle migration to all of them.
4. Migrate all remaining `style={}` color props.
5. Keep `boxShadow` in `style={}`.
6. Migrate non-color style props.

---

**IniciarVisaModal.tsx — dynamic error borders + accentColor:**
1. Delete `inputStyle` and `labelStyle`.
2. For fields with dynamic error borders:
   ```tsx
   className={`w-full bg-gj-input text-gj-text border rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none ${errors.campo ? 'border-gj-red' : 'border-white/10'}`}
   ```
3. Radio buttons with `accentColor: '#e8a020'`: replace with `className="accent-gj-amber cursor-pointer w-4 h-4"` (adjust sizing to match current values).
4. Date inputs: keep `style={{ colorScheme: 'dark' }}`.
5. Migrate all other color and non-color style props.
6. Modal backdrop/panel pattern applies.

---

**EditarVisaModal.tsx — dynamic error borders:**
Same approach as IniciarVisaModal. Apply conditional className for error borders. This file has 3 date inputs — all keep `style={{ colorScheme: 'dark' }}`.

---

After all 5 files are done, run `npm run build`.
  </action>
  <verify>
    <automated>cd /c/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -n "style={{" components/seminarios/AsistentesTable.tsx components/tramites/NuevoTramiteModal.tsx components/tramites/TramitesTable.tsx components/visas/IniciarVisaModal.tsx components/visas/EditarVisaModal.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "inputStyle" components/tramites/NuevoTramiteModal.tsx` outputs 0
    - `grep -c "inputStyle" components/visas/IniciarVisaModal.tsx` outputs 0
    - `grep -c "inputStyle" components/visas/EditarVisaModal.tsx` outputs 0
    - `grep -c "accent-gj-amber" components/visas/IniciarVisaModal.tsx` outputs at least 1 (radio button accent migrated)
    - `grep -c "border-gj-red" components/visas/IniciarVisaModal.tsx` outputs at least 1 (error border class)
    - `grep -c "border-gj-red" components/visas/EditarVisaModal.tsx` outputs at least 1
    - `grep -c "focus:ring-gj-amber" components/visas/IniciarVisaModal.tsx` outputs at least 1
    - `grep -c "#22c97a\|#e85a5a\|#e8a020\|#4a9eff\|#172645\|#111f38" components/tramites/TramitesTable.tsx` outputs 0
    - All remaining `style={{` in these 5 files contain ONLY colorScheme or boxShadow
  </acceptance_criteria>
  <done>5 files migrated across seminarios, tramites, visas: BADGE maps use class strings, error borders use conditional className, radio accent uses Tailwind class, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -rn "#[0-9a-fA-F]" components/seminarios/*.tsx components/tramites/*.tsx components/visas/*.tsx | grep "style=" | grep -v "colorScheme" | grep -v "boxShadow"` returns 0 lines
- All 11 files use gj-* Tailwind classes for colors
- All inputs have `focus:ring-2 focus:ring-gj-amber`
</verification>

<success_criteria>
All 11 files across components/seminarios/, components/tramites/, components/visas/ use exclusively Tailwind gj-* classes for colors. SeminarioCard uses Tailwind hover: variant (no JS state). BADGE maps use class strings. Error borders use conditional className. The only remaining style={} props are colorScheme: dark on date inputs and boxShadow on modals/dropdowns. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-hardening/09-04-SUMMARY.md`
</output>
