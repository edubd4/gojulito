---
phase: 06-bot-telegram-alfred
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/003_create_telegram_historial.sql
  - docs/n8n-alfred-setup.md
autonomous: true
requirements: [BOT-01, BOT-03]

must_haves:
  truths:
    - "Existe el archivo database/migrations/003_create_telegram_historial.sql con el CREATE TABLE y el índice correcto"
    - "La tabla telegram_historial tiene columnas: id BIGSERIAL, session_id TEXT NOT NULL, message JSONB NOT NULL, created_at TIMESTAMPTZ"
    - "Existe el archivo docs/n8n-alfred-setup.md en español con pasos numerados para importar el agente y configurar credenciales"
    - "El doc de setup incluye el SQL de la migración inline y un snippet de verificación"
    - "npm run build pasa sin errores"
  artifacts:
    - path: "database/migrations/003_create_telegram_historial.sql"
      provides: "telegram_historial table schema"
      contains: "telegram_historial"
    - path: "docs/n8n-alfred-setup.md"
      provides: "n8n Alfred setup guide"
      contains: "gojulitotestev1"
  key_links:
    - from: "database/migrations/003_create_telegram_historial.sql"
      to: "n8n memoryPostgresChat v1.3 node"
      via: "tableName: telegram_historial"
      pattern: "telegram_historial"
    - from: "docs/n8n-alfred-setup.md"
      to: "Gojulitofiles/agente_gojulito.json"
      via: "import instructions + credential reassignment"
      pattern: "gojulitotestev1"
---

<objective>
Completar la infraestructura del bot Telegram Alfred: crear la migración SQL para la tabla de memoria del agente y documentar el proceso de configuración en n8n.

Purpose: El nodo `memoryPostgresChat` v1.3 en `agente_gojulito.json` requiere la tabla `telegram_historial` para funcionar. Sin la migración, el agente n8n no arranca. El doc de setup cierra el gap operativo de BOT-03 — sin él, Julio no puede configurar Alfred en producción sin asistencia técnica.

Output: Archivo SQL de migración listo para ejecutar en Supabase SQL Editor + guía de setup en español con pasos numerados para importar el agente y conectar todas las credenciales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-bot-telegram-alfred/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear migración telegram_historial</name>
  <files>database/migrations/003_create_telegram_historial.sql</files>
  <action>
    Crear el archivo `database/migrations/003_create_telegram_historial.sql` con el header estándar del proyecto y el SQL exacto definido en D-01.

    El archivo debe seguir el formato de `database/migrations/002_add_activo_seminarios.sql`:
    - Primera línea: comentario con descripción de la migración
    - Segunda línea: aviso de ejecución en Supabase SQL Editor
    - Línea en blanco
    - SQL

    Contenido completo del archivo:

    ```sql
    -- Migración: crear tabla telegram_historial para memoria del agente n8n (Postgres Chat Memory v1.3)
    -- Ejecutar en Supabase SQL Editor

    CREATE TABLE IF NOT EXISTS telegram_historial (
      id         BIGSERIAL PRIMARY KEY,
      session_id TEXT NOT NULL,
      message    JSONB NOT NULL,
      created_at TIMESTAMPTZ DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS idx_telegram_historial_session_id
      ON telegram_historial (session_id);
    ```

    Restricciones (per D-02): NO agregar columnas adicionales. El nodo `memoryPostgresChat` gestiona esta tabla directamente — columnas extra pueden causar errores de inserción en n8n.

    El nombre de la tabla debe ser exactamente `telegram_historial` (sin variaciones) — el JSON del agente usa `tableName: "telegram_historial"` y no debe modificarse.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - Archivo `database/migrations/003_create_telegram_historial.sql` existe
    - Contiene `CREATE TABLE IF NOT EXISTS telegram_historial` con las 4 columnas exactas: `id BIGSERIAL`, `session_id TEXT NOT NULL`, `message JSONB NOT NULL`, `created_at TIMESTAMPTZ`
    - Contiene `CREATE INDEX IF NOT EXISTS idx_telegram_historial_session_id`
    - `npm run build` pasa sin errores de TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear guía de setup n8n Alfred</name>
  <files>docs/n8n-alfred-setup.md</files>
  <action>
    Crear `docs/n8n-alfred-setup.md` en español con 8 pasos numerados para configurar el agente Alfred en n8n. Formato markdown con secciones claras y notas de variables de entorno.

    Contenido del documento:

    ---
    # Configuración del Agente Alfred en n8n

    Guía para importar y activar el agente Telegram Alfred (`agente_gojulito.json`) en una instancia de n8n.

    ## Prerequisitos

    Antes de comenzar, asegúrate de tener:

    - Una instancia de n8n con acceso a la base de datos de Supabase (PostgreSQL)
    - Un bot de Telegram creado en [@BotFather](https://t.me/BotFather) con el token disponible
    - Acceso al Supabase SQL Editor del proyecto GoJulito
    - Las credenciales de conexión a Supabase (host, base de datos, usuario, contraseña)

    ---

    ## Paso 1: Prerequisitos de entorno

    Verificar que n8n tiene conectividad de red hacia Supabase. El agente usa conexión directa a PostgreSQL (no via API REST de Supabase).

    ## Paso 2: Aplicar la migración en Supabase

    Ejecutar el siguiente SQL en el **Supabase SQL Editor** del proyecto GoJulito:

    ```sql
    CREATE TABLE IF NOT EXISTS telegram_historial (
      id         BIGSERIAL PRIMARY KEY,
      session_id TEXT NOT NULL,
      message    JSONB NOT NULL,
      created_at TIMESTAMPTZ DEFAULT now()
    );

    CREATE INDEX IF NOT EXISTS idx_telegram_historial_session_id
      ON telegram_historial (session_id);
    ```

    **Verificación:** Confirmar que la tabla fue creada ejecutando:

    ```sql
    SELECT * FROM telegram_historial LIMIT 1;
    ```

    Si no retorna error, la migración fue aplicada correctamente.

    > **Nota:** Esta tabla es gestionada directamente por n8n. No agregar columnas adicionales.

    ## Paso 3: Importar el agente en n8n

    1. Abrir n8n y navegar a **Workflows**
    2. Hacer clic en **Add Workflow** → **Import from file**
    3. Seleccionar el archivo `Gojulitofiles/agente_gojulito.json`
    4. El workflow se importa con el nombre "GoJulito Alfred Agent" (o similar)

    > El JSON incluye todos los nodos preconfigurados: Telegram Trigger, AI Agent, Postgres Chat Memory, y todas las herramientas HTTP de GoJulito.

    ## Paso 4: Configurar credencial PostgreSQL

    El agente requiere una credencial PostgreSQL con el nombre `gojulitotestev1`.

    En n8n → **Credentials** → **Add Credential** → **PostgreSQL**:

    | Campo | Valor |
    |-------|-------|
    | Name | `gojulitotestev1` |
    | Host | Valor de `SUPABASE_DB_HOST` (panel Supabase → Settings → Database → Host) |
    | Database | `postgres` |
    | User | `postgres` |
    | Password | Contraseña de la base de datos de Supabase |
    | Port | `5432` (o `6543` para connection pooling) |
    | SSL | Activado |

    > **Importante:** Al importar el JSON, n8n no transfiere credenciales entre instancias. El nodo `Postgres Chat Memory` usa el credential id `CMXFQs4C0p2xTkc4` del JSON original — este id debe ser reasignado a la nueva credencial `gojulitotestev1` creada en este paso. Hacer clic en el nodo y seleccionar la credencial recién creada.

    ## Paso 5: Configurar credencial Telegram Bot

    En n8n → **Credentials** → **Add Credential** → **Telegram**:

    | Campo | Valor |
    |-------|-------|
    | Name | `gojulito_bot` |
    | Access Token | Token del bot obtenido de @BotFather |

    Asignar esta credencial al nodo **Telegram Trigger** y al nodo de respuesta de Telegram en el workflow.

    ## Paso 6: Configurar credencial OpenAI

    En n8n → **Credentials** → **Add Credential** → **OpenAI**:

    | Campo | Valor |
    |-------|-------|
    | Name | (cualquier nombre descriptivo, ej. `openai-gojulito`) |
    | API Key | Clave de API de OpenAI |

    Esta credencial se usa para el nodo de LLM (GPT) del AI Agent y para la transcripción de mensajes de audio de Telegram.

    ## Paso 7: Configurar GOJULITO_API_KEY en los nodos HTTP Request

    Cada nodo **HTTP Request** del agente (las herramientas que consultan GoJulito) requiere el header de autenticación `x-api-key`.

    **Valor a usar:** El valor de `N8N_API_KEY` en el archivo `.env.local` del proyecto GoJulito.

    Para cada nodo HTTP Request en el workflow:
    1. Abrir el nodo
    2. En **Headers** → agregar header:
       - **Name:** `x-api-key`
       - **Value:** el valor de `N8N_API_KEY`

    > **Nota de seguridad:** Este valor no debe compartirse. Es el secreto que autentica al bot contra los endpoints webhook de GoJulito.

    ## Paso 8: Activar el workflow

    1. Verificar que todos los nodos muestran sus credenciales asignadas (sin íconos de error)
    2. Hacer clic en **Activate** (toggle superior derecho del workflow)
    3. Enviar un mensaje de prueba al bot de Telegram
    4. Verificar en n8n → **Executions** que la ejecución fue exitosa

    ---

    ## Notas técnicas

    ### Mapeo de sesión (session_id)

    El nodo `Postgres Chat Memory` usa el `chat.id` de Telegram como identificador de sesión:

    ```
    sessionKey: ={{ $('Telegram Trigger').item.json.message.chat.id }}
    ```

    Este valor se almacena como `TEXT` en la columna `session_id` de `telegram_historial`. Cada conversación individual de Telegram tiene su propio historial separado.

    ### Ventana de contexto

    El parámetro `contextWindowLength: 20` limita el historial recuperado a las últimas 20 entradas por sesión. La tabla crece con el uso; no hay limpieza automática configurada.

    ---

    *Última actualización: 2026-03-24*
    ---

    Escribir este contenido en `docs/n8n-alfred-setup.md` (el directorio `docs/` ya existe y está vacío — per D-04).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    - Archivo `docs/n8n-alfred-setup.md` existe
    - Contiene 8 pasos numerados en español
    - Incluye el SQL de la migración inline como bloque de código
    - Incluye el snippet de verificación `SELECT * FROM telegram_historial LIMIT 1`
    - Menciona `gojulitotestev1` (nombre de credencial PostgreSQL)
    - Menciona el credential id `CMXFQs4C0p2xTkc4` y la necesidad de reasignarlo
    - Menciona `N8N_API_KEY` y el header `x-api-key`
    - Menciona `$('Telegram Trigger').item.json.message.chat.id` y `contextWindowLength: 20`
    - `npm run build` pasa sin errores
  </done>
</task>

</tasks>

<verification>
1. `database/migrations/003_create_telegram_historial.sql` existe con CREATE TABLE + CREATE INDEX
2. El nombre de la tabla es exactamente `telegram_historial` (sin variaciones)
3. Las 4 columnas son exactas: `id BIGSERIAL`, `session_id TEXT NOT NULL`, `message JSONB NOT NULL`, `created_at TIMESTAMPTZ DEFAULT now()`
4. `docs/n8n-alfred-setup.md` existe con 8 pasos numerados en español
5. El doc incluye el SQL inline y el snippet de verificación
6. El doc cubre todas las credenciales: PostgreSQL (`gojulitotestev1`), Telegram Bot (`gojulito_bot`), OpenAI, y `x-api-key`
7. `npm run build` pasa sin errores de TypeScript
</verification>

<success_criteria>
- La tabla `telegram_historial` puede crearse en Supabase ejecutando el archivo de migración sin errores
- El agente n8n puede conectarse a la tabla porque el nombre exacto `telegram_historial` coincide con `tableName` en el JSON del agente
- Julio puede seguir los 8 pasos del doc para configurar Alfred en n8n sin asistencia técnica adicional
- Todos los pasos críticos están documentados: importar el JSON, reasignar credenciales (incluyendo el id `CMXFQs4C0p2xTkc4`), y configurar el `x-api-key`
- `npm run build` pasa — ningún archivo TypeScript fue modificado, no se introducen errores de compilación
</success_criteria>

<output>
After completion, create `.planning/phases/06-bot-telegram-alfred/06-01-SUMMARY.md`
</output>
