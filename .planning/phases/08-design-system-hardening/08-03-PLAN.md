---
phase: 08-design-system-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - components/clientes/NuevoClienteModal.tsx
  - components/clientes/EditarClienteModal.tsx
  - components/clientes/RegistrarPagoModal.tsx
  - components/clientes/AgregarNotaModal.tsx
  - components/clientes/ClientesTable.tsx
  - components/clientes/ClientePagosTable.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-04
must_haves:
  truths:
    - "No hex color values exist in style={} props in any components/clientes/ file"
    - "All inputs and textareas in clientes modals have focus:ring-2 focus:ring-gj-amber visible focus indicator"
    - "BADGE maps in ClientesTable and ClientePagosTable use Tailwind class strings, not hex strings"
    - "Date inputs retain colorScheme: dark as the only remaining style={} for colors"
    - "Dynamic borderColor on error fields uses conditional className (border-gj-red vs border-white/10)"
  artifacts:
    - path: "components/clientes/NuevoClienteModal.tsx"
      provides: "Migrated nuevo cliente modal"
    - path: "components/clientes/EditarClienteModal.tsx"
      provides: "Migrated editar cliente modal"
    - path: "components/clientes/RegistrarPagoModal.tsx"
      provides: "Migrated registrar pago modal with conditional error border"
    - path: "components/clientes/AgregarNotaModal.tsx"
      provides: "Migrated agregar nota modal"
    - path: "components/clientes/ClientesTable.tsx"
      provides: "Migrated clientes table with BADGE maps as class strings"
    - path: "components/clientes/ClientePagosTable.tsx"
      provides: "Migrated cliente pagos table"
  key_links:
    - from: "components/clientes/ClientesTable.tsx"
      to: "tailwind.config.ts"
      via: "BADGE_ESTADO_CLIENTE/VISA/PAGO class strings reference gj-* tokens"
      pattern: "text-gj-green|text-gj-red|text-gj-amber|text-gj-blue"
    - from: "components/clientes/RegistrarPagoModal.tsx"
      to: "tailwind.config.ts"
      via: "Conditional border class references gj-red token"
      pattern: "border-gj-red"
---

<objective>
Migrate all 6 files in `components/clientes/` from inline `style={}` with hex colors to Tailwind `gj-*` classes.

Purpose: The clientes area is the highest-traffic operational section of GoJulito. Eliminating the dual styling system here ensures the most-used screens are fully consistent.
Output: 6 migrated component files with zero hex in style props (except colorScheme: dark on date inputs and boxShadow on modals/dropdowns). BADGE maps use Tailwind class strings. Dynamic error borders use conditional className.
</objective>

<execution_context>
@C:\Users\edu_i\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edu_i\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-hardening/08-CONTEXT.md
@.planning/phases/08-design-system-hardening/08-RESEARCH.md

<interfaces>
<!-- Hex-to-Tailwind mapping (locked in CONTEXT.md — per D-02): -->
| Hex / rgba | Tailwind class |
|---|---|
| `#0b1628` | `bg-gj-bg` |
| `#111f38` | `bg-gj-card` |
| `#172645` | `bg-gj-input` |
| `#e8a020` | `text-gj-amber` / `bg-gj-amber` / `border-gj-amber` / `accent-gj-amber` |
| `#22c97a` | `text-gj-green` / `bg-gj-green` |
| `#e85a5a` | `text-gj-red` / `bg-gj-red` / `border-gj-red` |
| `#4a9eff` | `text-gj-blue` / `bg-gj-blue` |
| `#e8e6e0` | `text-gj-text` |
| `#9ba8bb` | `text-gj-secondary` |
| `rgba(255,255,255,0.1)` | `border-white/10` |
| `rgba(255,255,255,0.07)` | `border-white/[7%]` |
| `rgba(255,255,255,0.12)` | `border-white/[12%]` |
| `rgba(155,168,187,0.35)` | `border-gj-secondary/35` |
| `rgba(0,0,0,0.55)` | `bg-black/55` |
| `rgba(232,160,32,0.15)` | `bg-gj-amber/15` |
| `rgba(232,160,32,0.30)` | `border-gj-amber/30` |
| `rgba(34,201,122,0.15)` | `bg-gj-green/15` |
| `rgba(34,201,122,0.25)` | `border-gj-green/25` |
| `rgba(232,90,90,0.15)` | `bg-gj-red/15` |
| `rgba(232,90,90,0.25)` | `border-gj-red/25` |
| `rgba(74,158,255,0.15)` | `bg-gj-blue/15` |
| `rgba(74,158,255,0.25)` | `border-gj-blue/25` |

<!-- Standard replacement classes (per D-02): -->
inputStyle object -> className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
labelStyle object -> className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
Date input: add style={{ colorScheme: 'dark' }} as the ONLY remaining style prop
Select: add cursor-pointer to the input class
accentColor: '#e8a020' -> accent-gj-amber in className

<!-- BADGE map migration pattern (ClientesTable has 3 maps): -->
BADGE_ESTADO_CLIENTE:
  PROSPECTO:  { classes: 'text-gj-secondary bg-gj-secondary/15', label: 'Prospecto' }
  ACTIVO:     { classes: 'text-gj-green bg-gj-green/15',         label: 'Activo' }
  FINALIZADO: { classes: 'text-gj-blue bg-gj-blue/15',           label: 'Finalizado' }
  INACTIVO:   { classes: 'text-gj-secondary bg-gj-secondary/15', label: 'Inactivo' }

BADGE_ESTADO_VISA:
  EN_PROCESO:      { classes: 'text-gj-amber bg-gj-amber/15',       label: 'En Proceso' }
  TURNO_ASIGNADO:  { classes: 'text-gj-blue bg-gj-blue/15',         label: 'Turno Asignado' }
  APROBADA:        { classes: 'text-gj-green bg-gj-green/15',       label: 'Aprobada' }
  RECHAZADA:       { classes: 'text-gj-red bg-gj-red/15',           label: 'Rechazada' }
  PAUSADA:         { classes: 'text-gj-red bg-gj-red/15',           label: 'Pausada' }
  CANCELADA:       { classes: 'text-gj-secondary bg-gj-secondary/15', label: 'Cancelada' }

BADGE_ESTADO_PAGO:
  PAGADO:    { classes: 'text-gj-green bg-gj-green/15',   label: 'Pagado' }
  DEUDA:     { classes: 'text-gj-red bg-gj-red/15',       label: 'Deuda' }
  PENDIENTE: { classes: 'text-gj-amber bg-gj-amber/15',   label: 'Pendiente' }

Badge render pattern:
<span className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${badge.classes}`}>{badge.label}</span>

<!-- Special cases (per D-05 — keep style={} for these): -->
1. colorScheme: 'dark' — date inputs ONLY
2. boxShadow with complex values — modals and dropdowns
3. gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))' — keep as style

<!-- ClientesTable dynamic select background (EXCEPTION per planning_notes): -->
The select element at ClientesTable line ~705 uses:
  style={{ backgroundColor: BADGE_ESTADO_CLIENTE[c.estado].bg }}
After BADGE migration, BADGE_ESTADO_CLIENTE will no longer have a .bg property.
Resolution: Remove the dynamic backgroundColor from the select style prop entirely.
The status is already visible via the badge <span> next to or above the select.
If the select needs styling, apply bg-gj-input as a static class.
This is the ONLY case where a dynamic style prop with runtime color cannot be migrated
to a static Tailwind class — so we eliminate the dynamic background on the select.

<!-- Dynamic error border pattern (RegistrarPagoModal): -->
Before: style={{ ...inputStyle, borderColor: errors.fecha ? '#e85a5a' : 'rgba(255,255,255,0.1)' }}
After:  className={`w-full bg-gj-input text-gj-text border rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none ${errors.fecha ? 'border-gj-red' : 'border-white/10'}`}

<!-- SVG stroke attributes in clientes/[id]/page.tsx: -->
SVG stroke="#e8a020" etc. are SVG element attributes (not CSS style= props).
DS-01 says "0 hex literals in style props" — SVG attributes are NOT style props.
Leave SVG stroke="..." attributes as-is (out of scope per DS-01 definition).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 clientes modals (NuevoClienteModal, EditarClienteModal, RegistrarPagoModal, AgregarNotaModal)</name>
  <files>
    components/clientes/NuevoClienteModal.tsx
    components/clientes/EditarClienteModal.tsx
    components/clientes/RegistrarPagoModal.tsx
    components/clientes/AgregarNotaModal.tsx
  </files>
  <read_first>
    - components/clientes/NuevoClienteModal.tsx
    - components/clientes/EditarClienteModal.tsx
    - components/clientes/RegistrarPagoModal.tsx
    - components/clientes/AgregarNotaModal.tsx
  </read_first>
  <action>
For each of the 4 files, apply the full migration pattern:

1. **Delete the `inputStyle` object** (and `labelStyle` if present). Replace every `style={inputStyle}` occurrence:
   - Standard inputs/selects/textareas:
     ```
     className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
     ```
   - For `<select>` elements: add `cursor-pointer` at end of className
   - For date inputs (`type="date"` or `type="datetime-local"`): use the className above AND keep `style={{ colorScheme: 'dark' }}`
   - For `{...inputStyle, colorScheme: 'dark'}` spreads: use the className above plus `style={{ colorScheme: 'dark' }}`

2. **Delete the `labelStyle` object** if present. Replace `style={labelStyle}`:
   ```
   className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
   ```

3. **Dynamic error border in RegistrarPagoModal** — replace the conditional borderColor style spread:
   ```tsx
   // Before:
   style={{ ...inputStyle, borderColor: errors.campo ? '#e85a5a' : 'rgba(255,255,255,0.1)' }}
   // After:
   className={`w-full bg-gj-input text-gj-text border rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none ${errors.campo ? 'border-gj-red' : 'border-white/10'}`}
   ```

4. **AgregarNotaModal textarea border** — `rgba(155,168,187,0.35)` border:
   Use `border border-gj-secondary/35` in className.

5. **Migrate all remaining `style={}` color props** using the mapping table in the interfaces block above.

6. **Migrate non-color style properties** to Tailwind utilities:
   - `fontSize: 14` -> `text-sm` | `fontSize: 12` -> `text-xs` | `fontSize: 11` -> `text-[11px]` | `fontSize: 18` -> `text-lg` | `fontSize: 20` -> `text-xl`
   - `fontWeight: 600` -> `font-semibold` | `fontWeight: 700` -> `font-bold` | `fontWeight: 500` -> `font-medium`
   - `padding: '8px 12px'` -> `px-3 py-2` | `padding: '24px 28px 16px'` -> `px-7 pt-6 pb-4`
   - `borderRadius: 8` -> `rounded-lg` | `borderRadius: 16` -> `rounded-2xl`
   - `gap: 10` -> `gap-2.5` | `gap: 12` -> `gap-3` | `gap: 8` -> `gap-2`
   - `fontFamily: 'DM Sans, sans-serif'` -> `font-sans`
   - `textTransform: 'uppercase'` -> `uppercase` | `letterSpacing: '0.05em'` -> `tracking-wide`
   - `display: 'flex'` -> `flex` | `flexDirection: 'column'` -> `flex-col`
   - `alignItems: 'center'` -> `items-center` | `justifyContent: 'flex-end'` -> `justify-end`
   - `position: 'fixed'` -> `fixed` | `inset: 0` -> `inset-0`
   - `zIndex: 60` -> `z-[60]` | `zIndex: 70` -> `z-[70]`
   - `width: '100%'` -> `w-full` | `minHeight: '100%'` -> `min-h-full`
   - `cursor: 'pointer'` -> `cursor-pointer` | `margin: 0` -> `m-0`

7. **Keep as `style={}`** ONLY:
   - `boxShadow` with complex values
   - `colorScheme: 'dark'` on date inputs

8. **Remove empty `style={}`** — if a style prop has no remaining properties after migration, delete the entire `style` attribute.

9. **Modal backdrop/panel pattern** (applied to any modal overlay):
   ```tsx
   {/* Backdrop */}
   <div className="fixed inset-0 z-[60] bg-black/55" onClick={() => onOpenChange(false)} />
   {/* Panel */}
   <div
     className="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-[70] bg-gj-card border border-white/10 rounded-2xl w-[90%] max-w-[480px] max-h-[90vh] overflow-y-auto font-sans"
     style={{ boxShadow: '0 24px 80px rgba(0,0,0,0.7)' }}
   >
   ```

Run `npm run build` after all 4 files are done.
  </action>
  <verify>
    <automated>cd /c/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -n "style={{" components/clientes/NuevoClienteModal.tsx components/clientes/EditarClienteModal.tsx components/clientes/RegistrarPagoModal.tsx components/clientes/AgregarNotaModal.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "inputStyle" components/clientes/NuevoClienteModal.tsx` outputs 0
    - `grep -c "inputStyle" components/clientes/EditarClienteModal.tsx` outputs 0
    - `grep -c "inputStyle" components/clientes/RegistrarPagoModal.tsx` outputs 0
    - `grep -c "inputStyle" components/clientes/AgregarNotaModal.tsx` outputs 0
    - `grep -c "focus:ring-gj-amber" components/clientes/NuevoClienteModal.tsx` outputs at least 1
    - `grep -c "focus:ring-gj-amber" components/clientes/RegistrarPagoModal.tsx` outputs at least 1
    - `grep -c "border-gj-red" components/clientes/RegistrarPagoModal.tsx` outputs at least 1 (error state border)
    - All remaining `style={{` in these 4 files contain ONLY colorScheme or boxShadow
  </acceptance_criteria>
  <done>4 clientes modal files have zero hex in style props, all inputs have focus rings, error borders use conditional className, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ClientesTable.tsx and ClientePagosTable.tsx (BADGE maps + table styles)</name>
  <files>
    components/clientes/ClientesTable.tsx
    components/clientes/ClientePagosTable.tsx
  </files>
  <read_first>
    - components/clientes/ClientesTable.tsx
    - components/clientes/ClientePagosTable.tsx
  </read_first>
  <action>
**ClientesTable.tsx — 3 BADGE maps + table styles:**

1. **Migrate BADGE_ESTADO_CLIENTE, BADGE_ESTADO_VISA, BADGE_ESTADO_PAGO** from hex strings to Tailwind class strings using the mapping in the interfaces block. Remove `.color` and `.bg` properties; replace with `.classes` (full className string).

   Update every JSX badge render site:
   ```tsx
   <span className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${badge.classes}`}>
     {badge.label}
   </span>
   ```

2. **Dynamic select background — REMOVE the dynamic style** (EXCEPTION documented in interfaces block):
   The `<select>` element that had `style={{ backgroundColor: BADGE_ESTADO_CLIENTE[c.estado].bg }}` should instead use the static class `bg-gj-input` in its className. Remove the dynamic `style` prop entirely. The status is visible via the badge span.

3. **`accentColor: '#e8a020'`** on checkboxes -> add `accent-gj-amber` to className, remove from style.

4. **Migrate all remaining `style={}` color props** in table headers, body cells, filter inputs, container divs using the hex-to-class mapping in the interfaces block.

5. **Filter inputs** (search, selects in filter bar): apply the same inputStyle migration pattern — `className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none cursor-pointer"`.

6. **Dropdown menus / action panels**: migrate color styles to classes; keep `boxShadow` as `style={}`.

7. **Page-level container** if present (`style={{ backgroundColor: '#0b1628', minHeight: '100%' }}`):
   Replace with `className="... bg-gj-bg min-h-full"`.

8. **Migrate non-color style props** to Tailwind utilities (same mapping as Task 1).

9. **Remove empty `style={}`** attributes.

---

**ClientePagosTable.tsx — inline BADGE pattern:**

1. Find BADGE map (inline object or top-level const). Migrate from hex strings to class strings using the same BADGE_ESTADO_PAGO pattern above.

2. Update badge render sites to use `className={...badge.classes...}`.

3. Migrate all other `style={}` color props and non-color props to Tailwind classes.

4. Keep `boxShadow` in `style={}` for dropdown menus.

Run `npm run build` after both files are done.
  </action>
  <verify>
    <automated>cd /c/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -n "style={{" components/clientes/ClientesTable.tsx components/clientes/ClientePagosTable.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep "BADGE_ESTADO_CLIENTE" components/clientes/ClientesTable.tsx | grep -c "classes"` outputs at least 1 (new format)
    - `grep -c "#22c97a\|#e85a5a\|#e8a020\|#4a9eff\|#9ba8bb\|#111f38\|#172645\|#e8e6e0" components/clientes/ClientesTable.tsx` outputs 0 (no hex literals)
    - `grep -c "#22c97a\|#e85a5a\|#e8a020\|#4a9eff" components/clientes/ClientePagosTable.tsx` outputs 0
    - `grep -c "accent-gj-amber" components/clientes/ClientesTable.tsx` outputs at least 1
    - All remaining `style={{` in both files contain ONLY boxShadow or colorScheme
  </acceptance_criteria>
  <done>ClientesTable and ClientePagosTable have BADGE maps using class strings, dynamic select bg replaced with static class, zero hex in style props, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -rn "#[0-9a-fA-F]" components/clientes/*.tsx | grep "style=" | grep -v "colorScheme" | grep -v "boxShadow"` returns 0 lines
- All 6 clientes files use gj-* Tailwind classes for colors
- All inputs have `focus:ring-2 focus:ring-gj-amber`
</verification>

<success_criteria>
All 6 files in components/clientes/ use exclusively Tailwind gj-* classes for colors. BADGE maps use class strings. Dynamic error borders use conditional className. The only remaining style={} props are colorScheme: dark on date inputs and boxShadow on modals/dropdowns. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-hardening/08-03-SUMMARY.md`
</output>
