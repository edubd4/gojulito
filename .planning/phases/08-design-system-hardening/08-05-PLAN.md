---
phase: 08-design-system-hardening
plan: 05
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - components/configuracion/GruposFamiliaresCard.tsx
  - components/configuracion/NuevoGrupoModal.tsx
  - components/configuracion/CrearUsuarioModal.tsx
  - components/configuracion/EditarUsuarioModal.tsx
  - components/configuracion/EditarNombreForm.tsx
  - components/configuracion/CambiarPasswordForm.tsx
  - components/configuracion/PreciosForm.tsx
  - components/configuracion/ToggleUsuario.tsx
  - components/configuracion/EliminarUsuarioBtn.tsx
  - components/grupos/AccionLoteGrupoModal.tsx
  - components/calendario/CalendarioView.tsx
  - components/dashboard/AccionesRapidas.tsx
  - app/(dashboard)/clientes/page.tsx
  - app/(dashboard)/tramites/page.tsx
  - app/(dashboard)/seminarios/page.tsx
  - app/(dashboard)/seminarios/[id]/page.tsx
  - app/(dashboard)/clientes/[id]/page.tsx
  - app/(dashboard)/configuracion/page.tsx
  - app/(dashboard)/pagos/page.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04
must_haves:
  truths:
    - "No hex color values exist in style={} props in any configuracion, grupos, calendario, dashboard, or app/(dashboard) file"
    - "CalendarioView CHIP, BADGE, and SEMINARIO_CHIP maps use Tailwind class strings"
    - "CalendarioView uses gj-seminario token for the purple seminario chip"
    - "All app/(dashboard)/ page root divs use bg-gj-bg min-h-full instead of style with backgroundColor"
    - "AccionesRapidas button colors use gj-* classes"
    - "All inputs with outline:none in configuracion forms have focus:ring-2 focus:ring-gj-amber"
  artifacts:
    - path: "components/calendario/CalendarioView.tsx"
      provides: "Migrated calendar with CHIP/BADGE maps as class strings, gj-seminario usage"
    - path: "app/(dashboard)/clientes/page.tsx"
      provides: "Page root div with bg-gj-bg instead of inline style"
    - path: "components/configuracion/PreciosForm.tsx"
      provides: "Migrated precios form with focus rings"
    - path: "components/grupos/AccionLoteGrupoModal.tsx"
      provides: "Migrated acciones grupales modal"
  key_links:
    - from: "components/calendario/CalendarioView.tsx"
      to: "tailwind.config.ts"
      via: "bg-gj-seminario and bg-gj-seminario/[18%] reference new gj-seminario token"
      pattern: "gj-seminario"
    - from: "app/(dashboard)/clientes/page.tsx"
      to: "tailwind.config.ts"
      via: "bg-gj-bg references gj-bg token"
      pattern: "bg-gj-bg"
---

<objective>
Migrate the final set of files: `components/configuracion/` (9 files), `components/grupos/AccionLoteGrupoModal.tsx`, `components/calendario/CalendarioView.tsx`, `components/dashboard/AccionesRapidas.tsx`, and all `app/(dashboard)/` pages with inline styles.

Purpose: This plan closes out the full migration, including the most complex file (CalendarioView with CHIP/BADGE/SEMINARIO_CHIP maps) and the page-level wrappers. After this plan, the entire codebase outside components/ui/ is 100% Tailwind gj-* for colors.
Output: All remaining files migrated. Zero hex in style props project-wide (except colorScheme: dark on date inputs and boxShadow on modals). DS-03 fully realized through gj-seminario usage in CalendarioView.
</objective>

<execution_context>
@C:\Users\edu_i\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edu_i\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-hardening/08-CONTEXT.md
@.planning/phases/08-design-system-hardening/08-RESEARCH.md

<interfaces>
<!-- Hex-to-Tailwind mapping (locked in CONTEXT.md — per D-02): -->
| Hex / rgba | Tailwind class |
|---|---|
| `#0b1628` | `bg-gj-bg` or `text-gj-bg` |
| `#111f38` | `bg-gj-card` |
| `#172645` | `bg-gj-input` |
| `#e8a020` | `text-gj-amber` / `bg-gj-amber` / `border-gj-amber` / `accent-gj-amber` |
| `#22c97a` | `text-gj-green` / `bg-gj-green` |
| `#e85a5a` | `text-gj-red` / `bg-gj-red` |
| `#4a9eff` | `text-gj-blue` / `bg-gj-blue` |
| `#a78bfa` | `text-gj-seminario` / `bg-gj-seminario` (registered in 08-01) |
| `#e8e6e0` | `text-gj-text` |
| `#9ba8bb` | `text-gj-secondary` |
| `rgba(255,255,255,0.1)` | `border-white/10` |
| `rgba(255,255,255,0.07)` | `border-white/[7%]` |
| `rgba(255,255,255,0.08)` | `border-white/[8%]` |
| `rgba(255,255,255,0.12)` | `border-white/[12%]` |
| `rgba(0,0,0,0.55)` | `bg-black/55` |
| `rgba(232,160,32,0.08)` | `bg-gj-amber/[8%]` |
| `rgba(232,160,32,0.15)` | `bg-gj-amber/15` |
| `rgba(34,201,122,0.15)` | `bg-gj-green/15` |
| `rgba(232,90,90,0.12)` | `bg-gj-red/[12%]` |
| `rgba(232,90,90,0.15)` | `bg-gj-red/15` |
| `rgba(74,158,255,0.12)` | `bg-gj-blue/[12%]` |
| `rgba(74,158,255,0.15)` | `bg-gj-blue/15` |
| `rgba(155,168,187,0.15)` | `bg-gj-secondary/15` |
| `rgba(167,139,250,0.18)` | `bg-gj-seminario/[18%]` |

<!-- Standard replacement classes (per D-02): -->
inputStyle -> className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
labelStyle -> className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
Date input: keep style={{ colorScheme: 'dark' }} as ONLY remaining style prop
Select: add cursor-pointer

<!-- Page-level container migration (Pattern 4 from research): -->
Before: <div className="p-4 sm:p-6 lg:p-8" style={{ backgroundColor: '#0b1628', minHeight: '100%' }}>
After:  <div className="p-4 sm:p-6 lg:p-8 bg-gj-bg min-h-full">

<!-- CalendarioView CHIP and BADGE maps (most complex migration in phase): -->
The file has these maps:
  CHIP: maps EstadoVisa to { bg, text } hex colors
  BADGE: maps payment status to { bg, text } hex colors
  SEMINARIO_CHIP: { bg: 'rgba(167,139,250,0.18)', text: '#a78bfa' }

Migrate each map from { bg: hex, text: hex } to { classes: string }:

CHIP (visa states):
  EN_PROCESO:      { classes: 'text-gj-bg bg-gj-amber font-semibold' }
  TURNO_ASIGNADO:  { classes: 'text-gj-text bg-gj-blue/15' }
  APROBADA:        { classes: 'text-gj-bg bg-gj-green font-semibold' }
  RECHAZADA:       { classes: 'text-gj-text bg-gj-red/15' }
  PAUSADA:         { classes: 'text-gj-text bg-gj-red/15' }
  CANCELADA:       { classes: 'text-gj-secondary bg-gj-secondary/15' }

BADGE (payment status on calendar items):
  PAGADO:    { classes: 'text-gj-green bg-gj-green/15' }
  DEUDA:     { classes: 'text-gj-red bg-gj-red/15' }
  PENDIENTE: { classes: 'text-gj-amber bg-gj-amber/15' }

SEMINARIO_CHIP (purple, uses the new gj-seminario token):
  Replace the inline object: { bg: 'rgba(167,139,250,0.18)', text: '#a78bfa' }
  Use: classes: 'text-gj-seminario bg-gj-seminario/[18%]'
  Or declare as: const SEMINARIO_CHIP_CLASSES = 'text-gj-seminario bg-gj-seminario/[18%]'

Update all JSX sites to use className={chip.classes} or className={SEMINARIO_CHIP_CLASSES}.

<!-- AccionesRapidas.tsx button colors: -->
The btnBase style object (layout properties) and individual button backgrounds:
  backgroundColor: '#e8a020' -> bg-gj-amber text-gj-bg (amber primary button)
  backgroundColor: 'rgba(74,158,255,0.12)' -> bg-gj-blue/[12%] text-gj-blue
  backgroundColor: 'rgba(34,201,122,0.12)' -> bg-gj-green/[12%] text-gj-green
  etc.
Migrate all button styles to Tailwind className. Keep boxShadow if present.

<!-- Properties that MUST stay as style={}: -->
1. colorScheme: 'dark' — date inputs only
2. boxShadow with complex values — keep as style
3. gridTemplateColumns: 'repeat(auto-fill, minmax(180px, 1fr))' — keep as style (no Tailwind equivalent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 9 configuracion files + AccionLoteGrupoModal + AccionesRapidas + app/(dashboard) pages</name>
  <files>
    components/configuracion/GruposFamiliaresCard.tsx
    components/configuracion/NuevoGrupoModal.tsx
    components/configuracion/CrearUsuarioModal.tsx
    components/configuracion/EditarUsuarioModal.tsx
    components/configuracion/EditarNombreForm.tsx
    components/configuracion/CambiarPasswordForm.tsx
    components/configuracion/PreciosForm.tsx
    components/configuracion/ToggleUsuario.tsx
    components/configuracion/EliminarUsuarioBtn.tsx
    components/grupos/AccionLoteGrupoModal.tsx
    components/dashboard/AccionesRapidas.tsx
    app/(dashboard)/clientes/page.tsx
    app/(dashboard)/tramites/page.tsx
    app/(dashboard)/seminarios/page.tsx
    app/(dashboard)/seminarios/[id]/page.tsx
    app/(dashboard)/clientes/[id]/page.tsx
    app/(dashboard)/configuracion/page.tsx
    app/(dashboard)/pagos/page.tsx
  </files>
  <read_first>
    - components/configuracion/GruposFamiliaresCard.tsx
    - components/configuracion/NuevoGrupoModal.tsx
    - components/configuracion/CrearUsuarioModal.tsx
    - components/configuracion/EditarUsuarioModal.tsx
    - components/configuracion/EditarNombreForm.tsx
    - components/configuracion/CambiarPasswordForm.tsx
    - components/configuracion/PreciosForm.tsx
    - components/configuracion/ToggleUsuario.tsx
    - components/configuracion/EliminarUsuarioBtn.tsx
    - components/grupos/AccionLoteGrupoModal.tsx
    - components/dashboard/AccionesRapidas.tsx
    - app/(dashboard)/clientes/page.tsx
    - app/(dashboard)/tramites/page.tsx
    - app/(dashboard)/seminarios/page.tsx
    - app/(dashboard)/seminarios/[id]/page.tsx
    - app/(dashboard)/clientes/[id]/page.tsx
    - app/(dashboard)/configuracion/page.tsx
    - app/(dashboard)/pagos/page.tsx
  </read_first>
  <action>
**configuracion/ files (9 files) — all follow the same inputStyle/labelStyle pattern:**

For each file:
1. Delete `inputStyle` object. Replace `style={inputStyle}` with:
   `className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"`
   Selects: add `cursor-pointer`. Date inputs: keep `style={{ colorScheme: 'dark' }}`.
2. Delete `labelStyle` object if present. Replace `style={labelStyle}` with:
   `className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"`
3. Migrate all remaining `style={}` color props (hex and rgba) to Tailwind classes using the mapping table.
4. Migrate non-color style props (fontSize, padding, gap, borderRadius, etc.) to Tailwind utilities.
5. Remove empty `style={}` attributes. Keep `boxShadow` as `style={}`.

Note: `PreciosForm.tsx` and `EditarNombreForm.tsx` have `inputStyle` without `boxSizing` — same migration applies.

---

**AccionLoteGrupoModal.tsx:**
1. Delete `inputStyle`. Apply standard className migration.
2. Migrate any inline BADGE objects to class strings using the payment status mapping.
3. Date inputs: keep `style={{ colorScheme: 'dark' }}`.
4. Keep `boxShadow` in `style={}`.
5. Migrate all other color and non-color style props.

---

**AccionesRapidas.tsx:**
1. Find the `btnBase` style object (layout + color properties) and individual button style objects.
2. Migrate color properties to gj-* classes. Migrate layout properties to Tailwind utilities.
3. Use these button color classes:
   - Amber primary button: `bg-gj-amber text-gj-bg hover:bg-gj-amber/90`
   - Blue secondary: `bg-gj-blue/[12%] text-gj-blue`
   - Green secondary: `bg-gj-green/[12%] text-gj-green`
   - (Adapt to exact colors found in the file)
4. Keep `boxShadow` as `style={}` if present.
5. Remove empty `style={}`.

---

**app/(dashboard)/ pages — page-level container migration:**

For every page file, find the root `<div>` with:
`style={{ backgroundColor: '#0b1628', minHeight: '100%' }}`

Replace with:
`className="... bg-gj-bg min-h-full"`
(Keep all existing className values, just add the two new classes and remove the style prop.)

For `app/(dashboard)/clientes/[id]/page.tsx`: this file may have additional `style={}` usage beyond the root container (SVG stroke attributes are excluded from DS-01 scope — leave SVG stroke="#..." attributes unchanged as they are SVG element attributes, not CSS style props). Migrate any non-SVG `style={}` color props.

After all files are done, run `npm run build`.
  </action>
  <verify>
    <automated>cd /c/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -rn "style={{" components/configuracion/ components/grupos/AccionLoteGrupoModal.tsx components/dashboard/AccionesRapidas.tsx "app/(dashboard)/" | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "inputStyle" components/configuracion/PreciosForm.tsx` outputs 0
    - `grep -c "inputStyle" components/configuracion/CrearUsuarioModal.tsx` outputs 0
    - `grep -c "inputStyle" components/grupos/AccionLoteGrupoModal.tsx` outputs 0
    - `grep -c "focus:ring-gj-amber" components/configuracion/PreciosForm.tsx` outputs at least 1
    - `grep -c "focus:ring-gj-amber" components/configuracion/CrearUsuarioModal.tsx` outputs at least 1
    - `grep -c "bg-gj-bg" "app/(dashboard)/clientes/page.tsx"` outputs at least 1
    - `grep -c "bg-gj-bg" "app/(dashboard)/pagos/page.tsx"` outputs at least 1
    - `grep -c "#0b1628\|#111f38\|#172645\|#e8a020\|#22c97a\|#e85a5a\|#4a9eff\|#e8e6e0\|#9ba8bb" components/configuracion/PreciosForm.tsx` outputs 0
    - All remaining `style={{` in configuracion/, grupos/AccionLoteGrupoModal.tsx, AccionesRapidas.tsx contain ONLY colorScheme, boxShadow, or gridTemplateColumns
  </acceptance_criteria>
  <done>All 9 configuracion files, AccionLoteGrupoModal, AccionesRapidas, and page containers migrated to Tailwind gj-* classes, focus rings added to all inputs, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CalendarioView.tsx (CHIP, BADGE, SEMINARIO_CHIP maps)</name>
  <files>
    components/calendario/CalendarioView.tsx
  </files>
  <read_first>
    - components/calendario/CalendarioView.tsx
  </read_first>
  <action>
CalendarioView.tsx is the most complex migration file. It contains calendar chip/badge maps that must be migrated carefully to avoid Tailwind purge issues.

**CRITICAL: Use complete static class strings in all map objects. Do NOT concatenate class strings from variables.**

1. **Find and migrate CHIP map** (maps EstadoVisa to colors for calendar chips):
   Replace { bg: hex, text: hex } format with { classes: string } format:
   ```typescript
   const CHIP: Record<EstadoVisa, { classes: string }> = {
     EN_PROCESO:     { classes: 'text-gj-bg bg-gj-amber font-semibold' },
     TURNO_ASIGNADO: { classes: 'text-gj-text bg-gj-blue/15' },
     APROBADA:       { classes: 'text-gj-bg bg-gj-green font-semibold' },
     RECHAZADA:      { classes: 'text-gj-text bg-gj-red/15' },
     PAUSADA:        { classes: 'text-gj-text bg-gj-red/15' },
     CANCELADA:      { classes: 'text-gj-secondary bg-gj-secondary/15' },
   }
   ```
   If the actual file uses different states or different visual intent (check the actual colors in the file), adapt the mapping accordingly — e.g., if EN_PROCESO uses amber as text-on-bg rather than bg color, use `text-gj-amber bg-gj-amber/15` instead. Match the visual intent of the original.

2. **Find and migrate BADGE map** (payment status chips):
   ```typescript
   const BADGE: Record<EstadoPago, { classes: string }> = {
     PAGADO:    { classes: 'text-gj-green bg-gj-green/15' },
     DEUDA:     { classes: 'text-gj-red bg-gj-red/15' },
     PENDIENTE: { classes: 'text-gj-amber bg-gj-amber/15' },
   }
   ```

3. **Migrate SEMINARIO_CHIP** (purple, uses the gj-seminario token added in plan 08-01):
   The original is: `{ bg: 'rgba(167,139,250,0.18)', text: '#a78bfa' }`
   Replace with a constant or inline class string:
   ```typescript
   const SEMINARIO_CHIP_CLASSES = 'text-gj-seminario bg-gj-seminario/[18%]'
   ```
   Or if it's used as part of a conditional structure, adapt appropriately.

4. **Update all JSX sites** that render these chips/badges:
   - Old: `style={{ color: chip.text, backgroundColor: chip.bg }}`
   - New: `className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${chip.classes}`}`
   - For SEMINARIO_CHIP: `className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${SEMINARIO_CHIP_CLASSES}`}`

5. **Migrate all other `style={}` color props** in the file — tooltip/popup overlays, card backgrounds, text colors, etc. Use the hex-to-class mapping table.

6. **Keep as `style={}`**:
   - `boxShadow` for tooltip/popup overlay (the research shows CalendarioView has complex tooltip overlays with boxShadow)
   - `gridTemplateColumns` if any grid layout uses custom value
   - `colorScheme: 'dark'` if any date inputs exist (CalendarioView probably has none, but check)

7. **Migrate non-color style props** (position, zIndex, fontSize, etc.) to Tailwind utilities.

8. **Remove empty `style={}`** attributes.

Run `npm run build` after migration.
  </action>
  <verify>
    <automated>cd /c/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -n "style={{" components/calendario/CalendarioView.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "#a78bfa\|rgba(167,139,250" components/calendario/CalendarioView.tsx` outputs 0 (seminario hex removed)
    - `grep -c "gj-seminario" components/calendario/CalendarioView.tsx` outputs at least 1 (new token used)
    - `grep "CHIP\|BADGE\|SEMINARIO" components/calendario/CalendarioView.tsx | grep -c "classes"` outputs at least 2 (maps migrated to class strings)
    - `grep -c "#22c97a\|#e85a5a\|#e8a020\|#4a9eff\|#111f38\|#172645\|#e8e6e0\|#9ba8bb" components/calendario/CalendarioView.tsx` outputs 0 (no hex literals)
    - All remaining `style={{` contain ONLY boxShadow or gridTemplateColumns
  </acceptance_criteria>
  <done>CalendarioView.tsx CHIP/BADGE/SEMINARIO_CHIP maps use Tailwind class strings, gj-seminario token used for purple chips, zero hex in style props, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -rn "#[0-9a-fA-F]" components/configuracion/ components/grupos/ components/calendario/ components/dashboard/AccionesRapidas.tsx "app/(dashboard)/" | grep "style=" | grep -v "colorScheme" | grep -v "boxShadow"` returns 0 lines
- `grep -c "gj-seminario" components/calendario/CalendarioView.tsx` returns at least 1
- All inputs in configuracion files have `focus:ring-gj-amber`
</verification>

<success_criteria>
All remaining files outside components/ui/ use exclusively Tailwind gj-* classes for colors. CalendarioView uses gj-seminario for the purple chip. All page root containers use bg-gj-bg. The only remaining style={} props project-wide are colorScheme: dark on date inputs, boxShadow on modals/popups, and gridTemplateColumns for custom grid layouts. Build passes. Phase 8 requirements DS-01, DS-02, DS-03, DS-04 are fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-hardening/08-05-SUMMARY.md`
</output>
