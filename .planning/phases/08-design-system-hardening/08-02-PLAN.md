---
phase: 08-design-system-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - components/pagos/NuevoPagoModal.tsx
  - components/pagos/PagosTable.tsx
  - components/pagos/DetallePagoModal.tsx
  - components/pagos/CambiarEstadoPagoDialog.tsx
  - components/pagos/FechaVencimientoDialog.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-04
must_haves:
  truths:
    - "No hex color values exist in style={} props in any components/pagos/ file"
    - "All inputs and textareas in pagos modals have focus:ring-2 focus:ring-gj-amber visible focus indicator"
    - "BADGE maps in PagosTable use Tailwind class strings, not hex strings"
    - "Date inputs retain colorScheme: dark as the only remaining style={} for colors"
  artifacts:
    - path: "components/pagos/NuevoPagoModal.tsx"
      provides: "Migrated nuevo pago modal"
    - path: "components/pagos/PagosTable.tsx"
      provides: "Migrated pagos table with BADGE classes"
    - path: "components/pagos/DetallePagoModal.tsx"
      provides: "Migrated detalle pago modal"
    - path: "components/pagos/CambiarEstadoPagoDialog.tsx"
      provides: "Migrated cambiar estado dialog"
    - path: "components/pagos/FechaVencimientoDialog.tsx"
      provides: "Migrated fecha vencimiento dialog"
  key_links:
    - from: "components/pagos/PagosTable.tsx"
      to: "tailwind.config.ts"
      via: "BADGE class strings reference gj-* tokens"
      pattern: "text-gj-green|text-gj-red|text-gj-amber|text-gj-blue"
---

<objective>
Migrate all 5 files in `components/pagos/` from inline `style={}` with hex colors to Tailwind `gj-*` classes. This is the component group with the highest style debt.

Purpose: Eliminate dual styling system in pagos components. After this plan, all pagos components follow the same pattern as Sidebar.tsx (100% Tailwind for colors).
Output: 5 migrated component files with zero hex in style props (except colorScheme: dark on date inputs and boxShadow on modals).
</objective>

<execution_context>
@C:\Users\edu_i\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\edu_i\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-hardening/08-CONTEXT.md
@.planning/phases/08-design-system-hardening/08-RESEARCH.md

<interfaces>
<!-- Hex-to-Tailwind mapping (from CONTEXT.md locked decisions): -->
| Hex / rgba | Tailwind class |
|---|---|
| `#0b1628` | `bg-gj-bg` |
| `#111f38` | `bg-gj-card` |
| `#172645` | `bg-gj-input` |
| `#e8a020` | `text-gj-amber` / `bg-gj-amber` / `border-gj-amber` / `accent-gj-amber` |
| `#22c97a` | `text-gj-green` / `bg-gj-green` |
| `#e85a5a` | `text-gj-red` / `bg-gj-red` / `border-gj-red` |
| `#4a9eff` | `text-gj-blue` / `bg-gj-blue` |
| `#e8e6e0` | `text-gj-text` |
| `#9ba8bb` | `text-gj-secondary` |
| `rgba(255,255,255,0.1)` | `border-white/10` |
| `rgba(255,255,255,0.07)` | `border-white/[7%]` |
| `rgba(0,0,0,0.55)` | `bg-black/55` |
| `rgba(232,160,32,0.08)` | `bg-gj-amber/[8%]` |
| `rgba(232,160,32,0.15)` | `bg-gj-amber/15` |
| `rgba(34,201,122,0.15)` | `bg-gj-green/15` |
| `rgba(232,90,90,0.08)` | `bg-gj-red/[8%]` |
| `rgba(232,90,90,0.15)` | `bg-gj-red/15` |
| `rgba(74,158,255,0.15)` | `bg-gj-blue/15` |

<!-- Standard replacement classes: -->
inputStyle object -> className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
labelStyle object -> className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
Date input: add style={{ colorScheme: 'dark' }} as the ONLY remaining style prop
Select: add cursor-pointer to the input class
accentColor: '#e8a020' -> className includes accent-gj-amber

<!-- BADGE map migration pattern: -->
Before: { color: '#22c97a', bg: 'rgba(34,201,122,0.15)', label: 'Pagado' }
After:  { classes: 'text-gj-green bg-gj-green/15', label: 'Pagado' }
Usage:  <span className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${badge.classes}`}>{badge.label}</span>

<!-- Modal container migration: -->
Backdrop: className="fixed inset-0 z-[60] bg-black/55"
Panel: className="fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 z-[70] bg-gj-card border border-white/10 rounded-2xl ..." style={{ boxShadow: '0 24px 80px rgba(0,0,0,0.7)' }}

<!-- Two properties that MUST stay as style={}: -->
1. colorScheme: 'dark' (date inputs)
2. boxShadow with complex values (modals)

<!-- Other CSS properties -> Tailwind: -->
fontSize: 14 -> text-sm | fontSize: 12 -> text-xs | fontSize: 11 -> text-[11px]
fontWeight: 600 -> font-semibold | fontWeight: 700 -> font-bold
padding: '8px 12px' -> px-3 py-2 | padding: '24px 28px 16px' -> px-7 pt-6 pb-4
borderRadius: 8 -> rounded-lg | borderRadius: 16 -> rounded-2xl | borderRadius: 14 -> rounded-[14px]
gap: 10 -> gap-2.5 | gap: 12 -> gap-3
fontFamily: 'DM Sans, sans-serif' -> font-sans
textTransform: 'uppercase' -> uppercase
letterSpacing: '0.05em' -> tracking-wide
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate NuevoPagoModal, DetallePagoModal, CambiarEstadoPagoDialog, FechaVencimientoDialog</name>
  <files>
    components/pagos/NuevoPagoModal.tsx
    components/pagos/DetallePagoModal.tsx
    components/pagos/CambiarEstadoPagoDialog.tsx
    components/pagos/FechaVencimientoDialog.tsx
  </files>
  <read_first>
    - components/pagos/NuevoPagoModal.tsx
    - components/pagos/DetallePagoModal.tsx
    - components/pagos/CambiarEstadoPagoDialog.tsx
    - components/pagos/FechaVencimientoDialog.tsx
    - components/dashboard/Sidebar.tsx (reference pattern for 100% Tailwind)
  </read_first>
  <action>
For each of the 4 files, apply the same migration pattern:

1. **Delete the `inputStyle` object** (and `labelStyle` if present). Replace every `style={inputStyle}` with:
   ```
   className="w-full bg-gj-input text-gj-text border border-white/10 rounded-lg px-3 py-2 text-sm font-sans focus:ring-2 focus:ring-gj-amber focus:outline-none"
   ```
   For `<select>` elements, add `cursor-pointer` to that className.
   For date inputs (`type="date"` or `type="datetime-local"`), keep `style={{ colorScheme: 'dark' }}` as the ONLY style prop.
   For inputs that had `{...inputStyle, colorScheme: 'dark'}`, use the className above PLUS `style={{ colorScheme: 'dark' }}`.

2. **Delete the `labelStyle` object** (if present). Replace every `style={labelStyle}` with:
   ```
   className="block text-xs font-semibold text-gj-secondary uppercase tracking-wide mb-1 font-sans"
   ```

3. **Migrate all remaining `style={}` props** that contain hex colors or rgba values:
   - `backgroundColor: '#111f38'` -> add `bg-gj-card` to className, remove from style
   - `backgroundColor: '#0b1628'` -> add `bg-gj-bg` to className, remove from style
   - `color: '#e8e6e0'` -> add `text-gj-text` to className, remove from style
   - `color: '#9ba8bb'` -> add `text-gj-secondary` to className, remove from style
   - `color: '#e8a020'` -> add `text-gj-amber` to className, remove from style
   - `color: '#22c97a'` -> add `text-gj-green` to className, remove from style
   - `color: '#e85a5a'` -> add `text-gj-red` to className, remove from style
   - `border: '1px solid rgba(255,255,255,0.1)'` -> add `border border-white/10` to className
   - `border: '1px solid rgba(255,255,255,0.07)'` -> add `border border-white/[7%]` to className
   - `borderBottom: '1px solid rgba(255,255,255,0.07)'` -> add `border-b border-white/[7%]` to className
   - `borderTop: '1px solid rgba(255,255,255,0.07)'` -> add `border-t border-white/[7%]` to className
   - `backgroundColor: 'rgba(232,160,32,0.08)'` -> add `bg-gj-amber/[8%]` to className
   - `backgroundColor: 'rgba(232,90,90,0.08)'` -> add `bg-gj-red/[8%]` to className
   - `backgroundColor: 'rgba(0,0,0,0.55)'` -> add `bg-black/55` to className
   - `accentColor: '#e8a020'` -> add `accent-gj-amber` to className, remove from style
   - `borderColor: '#e85a5a'` or conditional border colors -> use conditional className: `${errors.field ? 'border-gj-red' : 'border-white/10'}`

4. **Migrate non-color style properties** to Tailwind classes:
   - `fontSize: 14` -> `text-sm` | `fontSize: 12` -> `text-xs` | `fontSize: 11` -> `text-[11px]` | `fontSize: 20` -> `text-xl` | `fontSize: 16` -> `text-base` | `fontSize: 28` -> `text-[28px]`
   - `fontWeight: 600` -> `font-semibold` | `fontWeight: 700` -> `font-bold` | `fontWeight: 500` -> `font-medium`
   - `padding: '8px 12px'` -> `px-3 py-2` | `padding: '24px 28px 16px'` -> `px-7 pt-6 pb-4` | `padding: '20px 28px'` -> `px-7 py-5` | `padding: '16px 28px'` -> `px-7 py-4`
   - `borderRadius: 8` -> `rounded-lg` | `borderRadius: 16` -> `rounded-2xl` | `borderRadius: 14` -> `rounded-[14px]`
   - `gap: 10` -> `gap-2.5` | `gap: 12` -> `gap-3`
   - `fontFamily: 'DM Sans, sans-serif'` -> `font-sans` | `fontFamily: 'Fraunces, serif'` -> `font-display`
   - `textTransform: 'uppercase'` -> `uppercase`
   - `letterSpacing: '0.05em'` -> `tracking-wide`
   - `display: 'flex'` -> `flex` | `flexDirection: 'column'` -> `flex-col` | `alignItems: 'center'` -> `items-center` | `justifyContent: 'center'` -> `justify-center` | `justifyContent: 'flex-end'` -> `justify-end`
   - `position: 'fixed'` -> `fixed` | `position: 'absolute'` -> `absolute` | `inset: 0` -> `inset-0`
   - `zIndex: 60` -> `z-[60]` | `zIndex: 70` -> `z-[70]` | `zIndex: 20` -> `z-20`
   - `width: '100%'` -> `w-full` | `minHeight: '100%'` -> `min-h-full`
   - `maxHeight: '65vh'` -> `max-h-[65vh]` | `overflowY: 'auto'` -> `overflow-y-auto`
   - `cursor: 'pointer'` -> `cursor-pointer`
   - `margin: 0` -> `m-0`

5. **Keep as `style={}`** ONLY:
   - `boxShadow: '0 24px 80px rgba(0,0,0,0.7)'` (or similar complex shadow values)
   - `colorScheme: 'dark'` on date inputs
   - `boxSizing: 'border-box'` can be omitted (Tailwind's base reset already applies it globally)

6. **If a `style={}` prop becomes empty** after removing all properties, delete the entire `style` attribute.

7. **Success overlay pattern** (the green checkmark shown after save) — migrate to:
   ```tsx
   <div className="absolute inset-0 z-20 rounded-[14px] bg-gj-bg/[97%] flex flex-col items-center justify-center gap-3">
     <div className="w-[52px] h-[52px] rounded-full bg-gj-green/15 border-2 border-gj-green flex items-center justify-center">
       <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" className="text-gj-green">
         <polyline points="20 6 9 17 4 12" />
       </svg>
     </div>
     <p className="text-gj-green text-base font-semibold m-0 font-sans">...message...</p>
   </div>
   ```

Run `npm run build` after completing all 4 files.
  </action>
  <verify>
    <automated>cd C:/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -rn "style={{" components/pagos/NuevoPagoModal.tsx components/pagos/DetallePagoModal.tsx components/pagos/CambiarEstadoPagoDialog.tsx components/pagos/FechaVencimientoDialog.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "inputStyle" components/pagos/NuevoPagoModal.tsx` outputs 0
    - `grep -c "inputStyle" components/pagos/DetallePagoModal.tsx` outputs 0
    - `grep -c "inputStyle" components/pagos/CambiarEstadoPagoDialog.tsx` outputs 0
    - `grep -c "labelStyle" components/pagos/NuevoPagoModal.tsx` outputs 0
    - `grep -c "focus:ring-2" components/pagos/NuevoPagoModal.tsx` outputs at least 1
    - `grep -c "focus:ring-gj-amber" components/pagos/NuevoPagoModal.tsx` outputs at least 1
    - All remaining `style={{` in these 4 files contain ONLY colorScheme or boxShadow (no hex colors)
  </acceptance_criteria>
  <done>4 pagos modal/dialog files have zero hex in style props, all inputs have focus:ring-2 focus:ring-gj-amber, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate PagosTable.tsx (BADGE maps + table styles)</name>
  <files>components/pagos/PagosTable.tsx</files>
  <read_first>
    - components/pagos/PagosTable.tsx
    - .claude/skills/gojulito-design/SKILL.md (badge pattern reference)
  </read_first>
  <action>
1. **Migrate BADGE_ESTADO and BADGE_TIPO maps** from hex strings to Tailwind class strings:

   Before pattern:
   ```typescript
   const BADGE_ESTADO: Record<..., { color: string; bg: string; label: string }> = {
     PAGADO: { color: '#22c97a', bg: 'rgba(34,201,122,0.15)', label: 'Pagado' },
     ...
   }
   ```

   After pattern:
   ```typescript
   const BADGE_ESTADO: Record<..., { classes: string; label: string }> = {
     PAGADO:    { classes: 'text-gj-green bg-gj-green/15',       label: 'Pagado' },
     DEUDA:     { classes: 'text-gj-red bg-gj-red/15',           label: 'Deuda' },
     PENDIENTE: { classes: 'text-gj-amber bg-gj-amber/15',       label: 'Pendiente' },
   }
   ```

   For BADGE_TIPO (VISA / SEMINARIO):
   ```typescript
   const BADGE_TIPO: Record<..., { classes: string; label: string }> = {
     VISA:      { classes: 'text-gj-blue bg-gj-blue/15',         label: 'Visa' },
     SEMINARIO: { classes: 'text-gj-seminario bg-gj-seminario/[18%]', label: 'Seminario' },
   }
   ```

   Update all JSX that renders badges to use `className={...badge.classes...}` instead of `style={{ color: badge.color, backgroundColor: badge.bg }}`.

   Badge render pattern:
   ```tsx
   <span className={`inline-block px-2 py-0.5 rounded text-xs font-semibold ${badge.classes}`}>
     {badge.label}
   </span>
   ```

2. **Migrate all other style={} props** in PagosTable.tsx using the same hex-to-Tailwind mapping from Task 1. This includes:
   - Table header cells: `backgroundColor: '#111f38'` -> `bg-gj-card`, `color: '#9ba8bb'` -> `text-gj-secondary`, etc.
   - Table body cells: `color: '#e8e6e0'` -> `text-gj-text`
   - Filter inputs: same inputStyle migration
   - Dropdown menus: keep `boxShadow` in style, migrate colors to classes
   - Container divs with background colors

3. **Migrate non-color properties** (fontSize, padding, borderRadius, etc.) using the same mappings as Task 1.

4. **Date inputs**: Keep `style={{ colorScheme: 'dark' }}` only.

5. Run `npm run build` to verify.
  </action>
  <verify>
    <automated>cd C:/Users/edu_i/OneDrive/Documentos/Proyectos2026/gojulito && npm run build && grep -n "style={{" components/pagos/PagosTable.tsx | grep -v "colorScheme" | grep -v "boxShadow" | grep -v "gridTemplateColumns" || echo "PASS: no disallowed style props"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `grep -c "inputStyle" components/pagos/PagosTable.tsx` outputs 0
    - `grep "BADGE_ESTADO" components/pagos/PagosTable.tsx | grep -c "classes"` outputs at least 1 (confirms new format)
    - `grep -c "#22c97a\|#e85a5a\|#e8a020\|#4a9eff" components/pagos/PagosTable.tsx` outputs 0 (no hex color literals remain)
    - `grep -c "text-gj-green\|text-gj-red\|text-gj-amber\|text-gj-blue" components/pagos/PagosTable.tsx` outputs at least 4 (badge classes present)
    - All remaining `style={{` contain ONLY colorScheme, boxShadow, or gridTemplateColumns
  </acceptance_criteria>
  <done>PagosTable.tsx BADGE maps use Tailwind classes, zero hex in style props, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -rn "#[0-9a-fA-F]" components/pagos/*.tsx | grep "style=" | grep -v "colorScheme" | grep -v "boxShadow"` returns 0 lines
- All 5 pagos files use `gj-*` classes for colors
- All inputs have `focus:ring-2 focus:ring-gj-amber`
</verification>

<success_criteria>
All 5 files in components/pagos/ use exclusively Tailwind gj-* classes for colors. The only remaining style={} props are colorScheme: 'dark' on date inputs and boxShadow on modal containers. All BADGE maps use class strings. All inputs have visible focus rings. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-hardening/08-02-SUMMARY.md`
</output>
